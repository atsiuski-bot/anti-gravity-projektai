{
  "name": "workz-functions",
  "description": "WORKZ Cloud Functions — FCM push senders + Storage attachment cleanup",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "test": "node integrityScans.test.cjs && node decisionLog.test.cjs && node workDay.test.cjs",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
