{
  "counts": {
    "raw": 29,
    "deduped": 29,
    "verified": 29,
    "confirmed": 8
  },
  "tokens": {
    "find": 289166,
    "verify": 350788,
    "total": 639954
  },
  "agentCount": 98,
  "totalTokens": 5500249,
  "totalToolCalls": 1020,
  "confirmed": [
    {
      "title": "computeWorkerStats omits a finished plain task's own manualMinutes, undercounting totalHours/activeDays vs DailyStatistics and the CSV export",
      "file": "src/utils/workerStats.js",
      "line": "141-161, 284-292",
      "severity": "high",
      "detail": "computeWorkerStats builds totalWorkMin (and totalHours/totalWorkMinutes/activeDays/avgPerDay/normCoverage/productivePct/avgDayLength/weekConsistency) EXCLUSIVELY from summing `raw.workSessions` durations into `days[date].workMin` (lines 142-149, 160). It never adds a plain (non-quickWork, non-systemTask, non-timeChanged) finished task's own `manualMinutes` — the additive, session-less worked time that `calculateCurrentTotalMinutes` treats as real (manualMinutes + timerMinutes) and that a legacy/manually-entered task carries with NO backing work_sessions row.\n\nBoth sibling implementations that consume the exact same `raw.tasks` array explicitly add this: `aggregateDaily` in src/utils/reportAggregate.js (lines 183-204, with the comment 'summing sessions alone made the downloaded payroll timesheet report FEWER hours than the same manager sees on screen') and `DailyStatistics.jsx`'s own on-screen total (`manualTasks`/`totalManualMinutes`/`totalWorkedMinutes`, lines 775-809), guarded by the identical isSystemTask/isQuickWork/timeChanged checks. workerStats.js has no equivalent addition anywhere in the function, and this is locked in by its own test fixture (src/utils/workerStats.test.js lines 21-26, 43: two plain tasks carry `manualMinutes: 100` and `manualMinutes: 90` and the test still asserts `s.totalHours === 14` i.e. exactly the 480+360 session minutes, silently dropping 190 minutes of task-level manual time).\n\nImpact: computeWorkerStats is what Reports.jsx's on-screen 'Komandos suvestinė' / 'Mano suvestinė' cards (TeamPeriodSummary/PersonalPeriodSummary, via buildReport -> current.totalWorkMinutes) and the downloadable Markdown/JSON AI report's 'Viso dirbta' metric are built from. For any worker whose finished, non-quickwork/non-call task carries manualMinutes with no matching work_sessions row (the same legacy/manually-typed-time case the CSV and DailyStatistics guard was written for), these two on-screen/exported totals will show FEWER hours than DailyStatistics's own timeline for the identical period, and fewer than the downloaded CSV timesheet for the identical period. If that task is the ONLY work logged on a given day (no work_sessions row at all that day), the day is dropped from `days{}` entirely, so it also does not count toward activeDays/weekConsistency/avgPerDay/normCoverage/productivePct — a whole worked day can vanish from the period summary and the AI report while still fully appearing in DailyStatistics and the CSV export for the same range.",
      "dimension": "timetracking",
      "votesReal": 3,
      "votesTotal": 3,
      "confirmed": true,
      "reasons": [
        "CONFIRMED against source, and demonstrated by a discriminating test (not just read).\n\nMechanics (src/utils/workerStats.js): computeWorkerStats builds days[].workMin exclusively from raw.workSessions (lines 142-149), derives totalWorkMin from it (160), and every G1 metric plus avgDayLength/planCoveragePct hangs off totalWorkMin/activeDays (284-296, 299, 315). The token `manualMinutes` does not appear anywhere in the file; raw.tasks is used only for throughput/estimate/approval metrics (231-264). Both siblings consuming the same raw.tasks DO add a plain finished task's manualMinutes with identical isSystemTask/isQuickWork/timeChanged guards: aggregateDaily in src/utils/reportAggregate.js:183-204 (comment: summing sessions alone made the payroll CSV show FEWER hours than the screen) and DailyStatistics.jsx:775-809 (manualTasks -> totalManualMinutes -> totalWorkedMinutes). buildReport (reportAggregate.js:247-248) feeds computeWorkerStats into current.totalWorkMinutes/totalHours, which Reports.jsx renders at 1033-1043 (PersonalPeriodSummary) and 1220-1230 (TeamPeriodSummary); renderTimesheetCSV (reportAggregate.js:473) uses aggregateDaily. gatherReportData (src/utils/reportData.js:103,~140) pushes tasks into every worker bucket, and grep finds no manualMinutes synthesis in useWorkerStats.js/Reports.jsx/reportData.js, so nothing upstream compensates.\n\nChesterton's fence — NOT a design choice, an acknowledged gap: the very commit that added the aggregateDaily fix (f44aca3, 2026-07-22) says in its message under KNOWN INCOMPLETE: \"the manualMinutes ... fixes landed in aggregateDaily/CSV but computeWorkerStats still buckets work_sessions only, so one export can carry two totals\" — tracked for follow-up. The 2026-07-27 whole-app sweep (docs/audits/full-sweep-2026-07-27/00-SYNTHESIS.md, P2 item 11) lists \"Historical plain-task manual time is not valued consistently\" as a defect, not under accepted residual risks; the memory register calls it deliberately deferred P2. `git log -S manualMinutes -- src/utils/workerStats.js` shows only b07363a and f44aca3; the one later commit (1e3b522) merely swapped the day-bucketing helper. Prior rejected findings mentioning computeWorkerStats/manualMinutes are different claims (timeAdjustments double-count; Date-vs-ISO). Writers check rules out the reverse reading (CSV side double-counting): the modern pause flow (taskActions.js:224-241) only preserves an existing manualMinutes while banking elapsed time into timerMinutes; the only setters of manualMinutes=durationMinutes are the quick-work/call auto-logs (sessionActions.js:792/884, timerTransitionPlan.js:1061/1140) which the guards exclude; sessionEditActions.js:180-194 zeroes it and stamps timeChanged.\n\nDemonstrated: a temporary vitest file (created, run, removed; worktree left clean) with ONE plain task {status:'completed', completedAt 2026-06-10, manualMinutes:120} and zero work_sessions for June: computeWorkerStats -> totalWorkMinutes 0, totalHours null, activeDays null; buildReport.team.totalWorkMinutes 0 / totalHours 0; renderTimesheetCSV for the same worker+window -> row \"W,2026-06-10,02:00\" and Viso 02:00. All assertions passed. The existing fixture in workerStats.test.js (manualMinutes 100 + 90 on plain tasks, totalHours asserted 14) indeed locks the omission in.\n\nCalibration: the issue is real and the impact chain (period summary cards + Markdown/JSON AI report undercount vs DailyStatistics and CSV; a manual-only day vanishes from activeDays/avgPerDay/normCoverage/productivePct) is exactly as claimed. Severity caveat: the project's own triage rated it P2, and it affects legacy/typed-in tasks (modern timer flows never create a session-less manualMinutes on a plain task), so \"high\" overstates the day-to-day blast radius; the inconsistency itself is unambiguous.",
        "CONFIRMED REAL, and explicitly NOT a deliberate design choice — the Chesterton's-fence check fails in the claim's favour.\n\nCode (read at src/utils/workerStats.js): `days{}` is populated ONLY from `raw.workSessions` (lines 142-149) and `raw.breakSessions` (150-154); `totalWorkMin` = sum of `days[k].workMin` (160). `raw.tasks` is used only for throughput/estimate/approval metrics (231-264). No `manualMinutes` addition exists anywhere in the function. Consequences the claim lists all follow: totalHours/totalWorkMinutes/avgPerDay/normCoverage/productivePct (284-296) undercount, and a day whose only work is a session-less manualMinutes task never enters `days{}`, so activeDays/weekConsistency also miss it.\n\nSiblings (read): `aggregateDaily` in src/utils/reportAggregate.js:174-193 adds plain-task manualMinutes with the identical isSystemTask/isQuickWork/timeChanged guards and the comment \"summing sessions alone made the downloaded payroll timesheet report FEWER hours than the same manager sees on screen\". DailyStatistics.jsx:775-809 does the same on-screen (manualTasks -> totalManualMinutes -> totalWorkedMinutes). timeUtils.js:183 confirms `calculateCurrentTotalMinutes = manualMinutes + timerMinutes` (additive, not a duplicate of sessions).\n\nSelf-inconsistency within ONE artifact: buildReport (reportAggregate.js:247-290) feeds the SAME `raw.tasks` to computeWorkerStats (no manualMinutes -> `current.totalWorkMinutes`) and to aggregateDaily (with manualMinutes -> `daily[]`). Reports.jsx:1031-1046 and 1218-1233 render `w.totalWorkMinutes`/`t.totalWorkMinutes` as the on-screen 'Mano suvestinė'/'Komandos suvestinė' Veikla/Viso triplet — the comment there even says \"from the same aggregator ... so the merged card never disagrees with itself\", yet it disagrees with DailyStatistics and the CSV for the same range.\n\nDecisive fence evidence: `git log -S manualMinutes` shows the fix landed in f44aca3 (2026-07-22), whose commit message states verbatim under \"KNOWN INCOMPLETE (real improvements, but the finding is not fully closed — tracked for follow-up)\": \"the manualMinutes and payRateId fixes landed in aggregateDaily/CSV but computeWorkerStats still buckets work_sessions only, so one export can carry two totals\". The author acknowledged the gap and deferred it. The only later commit touching workerStats.js (1e3b522, 2026-08-05) is the work-day filing change; the follow-up never landed. No ADR/decisions-log entry re-classifies it as accepted (grep of docs/ finds only an unrelated ADR-0015 mention). The file's own header (line 15-16) states the intent to \"agree with Reports/DailyStatistics to the minute\", which the code contradicts.\n\nTest lock-in confirmed: workerStats.test.js:21-22 fixture tasks carry manualMinutes 100 and 90; line 43 asserts totalHours === 14 (exactly 480+360 session minutes), silently dropping 190 min.\n\nScope note (does not change the verdict): the affected population is plain tasks with manualMinutes and no backing work_sessions row — per the reportAggregate comment \"typically a legacy task whose actualTime was typed in\" — the same population the DailyStatistics/CSV guards were written for, so the discrepancy is live in prod data.",
        "CONFIRMED by reading the source and by an executed repro through the real aggregators (temporary vitest file, deleted afterwards; worktree left clean).\n\nSource facts (src/utils/workerStats.js): `days[k].workMin` is fed only by `raw.workSessions` (lines 142-149); `totalWorkMin` = sum of those buckets (160); `totalHours`/`totalWorkMinutes`/`activeDays`/`avgPerDay`/`normCoverage`/`productivePct` all derive from it (284-296). `raw.tasks` is used only for throughput/estimate metrics (231-264); `manualMinutes` is never read anywhere in the module. Both siblings that consume the same task array DO add it with identical guards: `aggregateDaily` in reportAggregate.js:183-204 and DailyStatistics.jsx:775-809 (also CombinedHoursSummary.jsx:191-197).\n\nRepro (one plain finished task, manualMinutes 90, no work_sessions row, window 2026-06): computeWorkerStats -> totalHours=null, totalWorkMinutes=0, activeDays=null, completedCount=1. buildReport team.totalWorkMinutes=0. The SAME buildReport with includeDaily produced daily=[{2026-06-10, workMinutes:90}], so the downloaded Markdown report prints \"Viso dirbta: 0m\" and, a few lines below, \"2026-06-10: veikla 1h 30m\". renderTimesheetCSV for the same input prints 01:30. So the divergence is internal to one artifact, not just cross-surface, and the whole worked day vanishes from activeDays.\n\nChesterton's fence checked — no deliberate design reason found: (1) workerStats.js's own header (line 15-16) and the totalWorkMinutes comment (285-287) claim it \"agrees with Reports/DailyStatistics to the minute\" / \"matching the timeline's own totals\" — the stated intent is the opposite of the behaviour. (2) Commit cf9d92d replaced DailyStatistics's own Veikla/Pertraukos/Viso bar (which counts manualMinutes) with the aggregator in the manager range view and suppressed the bar, on the premise \"same numbers, two sources\" — the premise was false for this task class, so the on-screen manager total silently changed source. (3) Commit f44aca3 added manualMinutes to the CSV because summing sessions alone made it show \"FEWER hours than the same manager sees on screen\"; its comment lists DailyStatistics and CombinedHoursSummary as the on-screen surfaces that count it and simply omits the Reports summary card / computeWorkerStats — an oversight, not an exclusion decision. (4) ADR 0009's \"a task with real sessions contributes through sessions, not its manualMinutes\" is the double-count guard (mirrored by the timeChanged/isQuickWork/isSystemTask checks) and says nothing about dropping manualMinutes when NO session exists; DailyStatistics, the ADR's reference surface, counts it. (5) The 2026-07-27 audit synthesis P2 #11 already records this exact inconsistency (\"Historical plain-task manual time is not valued consistently... total-hours/earnings aggregates use canonical work_sessions only\") and the remediation memory lists it as \"deliberately deferred, all P2\" — i.e. acknowledged as an open defect and postponed, not designed.\n\nCaveats for the orchestrator: this is a KNOWN open finding (2026-07-27 sweep, P2, deferred), so it is real but not new — dedupe against that register; the prior sweep rated it P2 rather than high. Data prevalence: no current UI types actualTime/manualMinutes onto a plain task (grep found no such input), so affected rows are legacy ones back-filled by pauseTask's backwards-compat path; their existence in prod is asserted by the prior audit and the CSV-fix rationale, not measured by me (Firebase MCP timed out this session). The 16h-clamp/earnings distinction in reportAggregate.js:88-93 and :450-458 is a separate, documented divergence about the money figure and does not justify the hours omission."
      ]
    },
    {
      "title": "Every active user's Firestore read exposes every colleague's payRate (salary tiers) and other admin-only control fields — contradicts the project's own documented threat model",
      "file": "firestore.rules",
      "line": "350-351",
      "severity": "high",
      "detail": "The /users/{userId} read rule is `allow read: if isAuthenticated() && (request.auth.uid == userId || isUserActive());`. Any authenticated, non-disabled worker (not just a manager/admin) can read the FULL document of any other user — there is no field-level restriction. That full document includes `payRate` (the NET hourly-rate tier table set by an admin, ADR 0012), plus `canBackdateTime`, `canEditOwnStartTime`, `isTest`, `teamManagerIds`/`overseerIds`/`seniorManagerIds`. This is not just a theoretical rules permissiveness: `src/context/UsersContext.jsx` (lines 30-45) does `onSnapshot(collection(db, 'users'), ...)` and spreads every field of every user doc (`{ id: doc.id, ...doc.data() }`) into app-wide React state (`users`/`usersMap`), and `UsersProvider` is mounted at the top of the whole app in `src/App.jsx` (line 77) above the single shared `/` route used by both workers and managers — so it runs for every logged-in user regardless of role, not just inside an admin-gated view. `UserManagement.jsx` (which is admin-only in the UI) simply reads `user.payRate` off this already-fetched, unfiltered array — but the underlying Firestore read that put `payRate` into every browser's memory happened before any role check, and is fully replayable by anyone with a valid session via the SDK/devtools regardless of which screens render.\n\nThis directly contradicts the project's own documented policy: `docs/security/threat-model-checklist.md` (lines 52-54, 101-106) explicitly lists `payRate` as a 'Read-restricted field' that 'must be checked — broad-read collections can still over-expose a sensitive field on the user doc,' and `docs/adr/0012-tiered-pay-rates-earnings.md` (line 51) states the intent as 'A worker can still read their OWN payRate' — not a colleague's. The actual rule was never scoped to enforce that: it only ever gated *writes* to payRate (`isAdmin() || payRate unchanged` on update, line ~434), never reads. Net effect: any worker can pull every teammate's negotiated salary tier and a handful of internal fraud-control flags out of a payroll/time-tracking app that explicitly intended those fields to be private.",
      "dimension": "security",
      "votesReal": 3,
      "votesTotal": 3,
      "confirmed": true,
      "reasons": [
        "CONFIRMED against source, and it is NOT a deliberate design choice — it is an already-logged, never-dispositioned P1 finding.\n\nEvidence (all verified by reading the worktree):\n- firestore.rules:350-351 is exactly `allow read: if isAuthenticated() && (request.auth.uid == userId || isUserActive());` — any active user reads any colleague's FULL user doc. payRate lives on that doc (create pin :374, update pin :434, written by UserManagement.jsx:998 via updateDoc(users/{id}, {payRate})). No users_public/profile collection exists; Firestore cannot restrict fields within an allowed read.\n- src/context/UsersContext.jsx:31-45 does onSnapshot(collection(db,'users')) and spreads every field into app state for every authenticated user; UsersProvider is mounted at src/App.jsx:77 above the single shared route, so the read fires for workers, not just admin views.\n- docs/security/threat-model-checklist.md:53-54 explicitly names payRate a \"Read-restricted field\" that \"must be checked — broad-read collections can still over-expose a sensitive field on the user doc\"; checklist item 4 says reads are intentionally broad BUT \"don't loosen a genuinely restricted field\" — i.e. the project's own read-broad doctrine carves payRate OUT. ADR 0012:51 states the intent as a worker reading their OWN payRate.\n\nChesterton's-fence check (why this is not an accepted design):\n- The \"read broad, write scoped\" doctrine (memory + checklist) exists because rules are not row filters and client-side-filtered features would break. That justifies broad reads on tasks/sessions/work_hours; the docs explicitly exempt payRate from it.\n- The project's own 2026-07-10 full sweep already confirmed this exact issue as R-10 \"Pay rates are disclosed to every active user\" (docs/audits/full-sweep-2026-07-10/00-reasoning-confirmed.md:72-76; 00-SYNTHESIS.md:51, P1, effort L; 04-coverage-matrix.md:17, coverage Absent). The 2026-06-21 sweep had explicitly handed the broad-users-read judgement to the security lens, and that lens ruled it a P1.\n- Disposition trail: P0s fixed (a2b1da1), R-08 and R-12 fixed later, R-04 + R-06-create FORMALLY accepted via ADR 0021. R-10 appears in no ADR, no decisions-log entry, no accepted-risk register, no later audit (07-21, 07-27, 08-07) and no memory note — it is an open P1 backlog item, not a decision.\n- Emulator suite has oracles only for payRate WRITE pins (securityRules.integration.test.js:141,776); none for cross-user read, matching the coverage-matrix \"Absent\".\n\nNuance for remediation (does not refute the finding): manager/admin surfaces legitimately need subordinates' payRate — TaskModal.jsx:400-404 (manager picks assignee tariff, isManager-gated in UI only), reportData.js:190 (Reports earnings), UserManagement.jsx (admin). EarningsBreakdown uses the caller's OWN userData.payRate. So the fix must scope compensation reads to owner + overseer/admin (a data-model move, effort L as the audit said), not owner-only. The claim's secondary fields (teamManagerIds/overseerIds) are lower-sensitivity and partly needed client-side for scope filtering; the load-bearing exposure is payRate, and [security/high] matches the project's own P1 rating.",
        "CONFIRMED against source. firestore.rules:350-351 reads exactly `allow read: if isAuthenticated() && (request.auth.uid == userId || isUserActive());` — the only cross-user gate is \"active\", and Firestore cannot restrict fields within an allowed document read, so every active worker can read every colleague's full doc incl. `payRate` (write-pinned admin-only at :434 but never read-scoped), `canBackdateTime`, `canEditOwnStartTime`, `isTest`, `teamManagerIds`/`overseerIds`/`seniorManagerIds`. src/context/UsersContext.jsx:30-36 does `onSnapshot(collection(db,'users'))` and spreads `...doc.data()` into app-wide state; `UsersProvider` is mounted in src/App.jsx above the single `/` route, so the read runs for every logged-in user regardless of role. Chesterton's fence checked and it does NOT excuse this: the broad `/users` read is deliberate (ADR 0005 §6 \"the `users` roster keep their broad READ\"; rules L134-144 doctrine), but that decision (2026-06-22) concerns the public roster/shift calendar and predates `payRate` landing on the user doc (ADR 0012, 2026-06-23). ADR 0012 only added a write pin and its own text (\"A worker can still read their OWN payRate\") shows own-read was the intent; docs/security/threat-model-checklist.md:53-54 explicitly classifies `payRate` as a read-restricted field and warns of exactly this over-exposure. Decisive: the project's own 2026-07-10 full sweep already confirmed this identical issue as R-10 \"Pay rates are disclosed to every active user\" (docs/audits/full-sweep-2026-07-10/00-reasoning-confirmed.md:72-76, P1 effort L, fix = move compensation to an owner/admin-scoped document), coverage matrix marks its test \"Absent\", and the remediation record states P1/P2 R-08…R-43 were NOT touched; only R-04 and R-06-create were formally accepted as risks (ADR 0021). No later ADR, decisions-log entry or audit closes or accepts R-10, and `git log -S payRate -- firestore.rules` shows no read-side change since. So: real, known-open P1, not an accepted or deliberate trade-off — it is a duplicate of R-10 rather than a novel finding. Fix nuance: managers legitimately read the assignee's `payRate.rates` client-side to pick a tariff (src/components/TaskModal.jsx:400-404), so the remedy must be the R-10 data-model split (owner + overseer/admin readable compensation doc), not a naive owner-only tighten of the `/users` read rule.",
        "CONFIRMED real, and NOT a deliberate design choice — it is a previously-confirmed, still-open P1 (R-10) that no fix or accepted-risk register ever closed.\n\nEvidence:\n1. Source verified verbatim: firestore.rules:350-351 is `allow read: if isAuthenticated() && (request.auth.uid == userId || isUserActive());` with isUserActive() (rules:26-30) = any authenticated caller whose own users doc lacks isDisabled:true — i.e. every plain active worker. Firestore rules cannot project fields on an allowed read, so the whole doc (payRate, canBackdateTime, canEditOwnStartTime, isTest, overseerIds, teamManagerIds) is returned. The payRate protection at rules:428-438 is UPDATE-only, exactly as the claim says.\n2. Reproduced against the REAL rules in the Firestore emulator (firebase emulators:exec + @firebase/rules-unit-testing, temp script since deleted): principal `worker-a` (role worker, active) getDoc(users/worker-b) SUCCEEDED and returned payRate {\"tiers\":[{upToHours:80,netRate:9.5},{upToHours:null,netRate:12}]} plus canBackdateTime=true and overseerIds; getDocs(collection 'users') SUCCEEDED with payRate PRESENT on the colleague doc. Controls behaved as the rule comment documents: disabled user DENIED, unauthenticated DENIED. So the boundary is exactly \"active vs disabled\", never \"own vs colleague\".\n3. Client path verified: src/context/UsersContext.jsx:30-45 does onSnapshot(collection(db,'users')) and spreads every field into app-wide state; UsersProvider is mounted in src/App.jsx:77 above the single '/' route shared by workers and managers — the read happens for every role, before any UI gate.\n4. Chesterton's fence checked and does not apply to payRate: the documented \"read broad, write scoped\" doctrine (memory + threat-model item 4) covers the time collections and the public profile (names/roles/scope needed for UserChip/teamScope). The project's OWN threat model (docs/security/threat-model-checklist.md:53-54) names payRate as the read-restricted exception that \"broad-read collections can still over-expose\", and ADR 0012 line 51 states the intent as a worker reading their OWN payRate. No worker-facing surface consumes a colleague's payRate: TaskTimerControls.jsx:611/868 and EarningsBreakdown.jsx:32 read `userData?.payRate` (own doc); TaskModal.jsx:404 rate picker is `isManager`-gated; reportData/reportAggregate are manager reports; UserManagement is admin-only. So broad read is not needed for this field.\n5. Prior-audit status: this is R-10 \"Pay rates are disclosed to every active user\" in docs/audits/full-sweep-2026-07-10 (00-reasoning-confirmed.md:72-76; synthesis P1, effort L; coverage matrix marks the test \"Absent\"). The remediation memory records P0s + R-08 + R-12 fixed and ONLY R-04 + R-06-create formally accepted; the 2026-07-27 and 2026-08-07 fix/reject lists never mention R-10/payRate. It is therefore an open P1, not an accepted residual.\n\nCaveats on the claim's framing (do not change the verdict): overseerIds/teamManagerIds are structural scope data the client legitimately needs and are low-sensitivity, so the severity rests on payRate (plus the minor canBackdateTime/isTest flags). The prior audit rated the fix effort L because rules cannot hide fields — closing it requires a data-model change (owner/admin-scoped compensation doc or subcollection), not a rules edit."
      ]
    },
    {
      "title": "Bespoke icon-only remove-attachment button duplicates IconButton in the same file",
      "file": "src/components/TaskModal.jsx",
      "line": "1996-2003",
      "severity": "medium",
      "detail": "The photo-removal control is a raw `<button>` hand-rolling IconButton's exact contract (`inline-flex items-center justify-center min-h-touch min-w-touch rounded-full ... focus-visible:ring-2 focus-visible:ring-brand-ring focus-visible:ring-offset-2`) instead of `<IconButton icon={Trash2} label=\"Pašalinti nuotrauką\" ... />`. This is not a missing-tool situation: `IconButton` is already imported in this same file (line 29) and used for other controls (lines 1518, 2072, 2229-2253), so this instance is a straight drift from the canonical component, not a case where IconButton lacked support for the need.",
      "dimension": "discipline",
      "votesReal": 3,
      "votesTotal": 3,
      "confirmed": true,
      "reasons": [
        "Confirmed by reading src/components/TaskModal.jsx:1996-2003 in the worktree. The photo-remove control is a raw <button> carrying `inline-flex items-center justify-center min-h-touch min-w-touch ... focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-ring focus-visible:ring-offset-2` plus aria-label — the same base contract src/components/ui/IconButton.jsx emits (and its `danger` variant already supplies `hover:bg-feedback-danger-soft`). IconButton is imported at line 29 and used 6 times in this file, including `<IconButton icon={Trash2} variant=\"danger\" .../>` for template deletion at 2243-2248, so this is the sole Trash2 icon-only control in the file not on the canonical component.\n\nChesterton's-fence check finds no fence: (1) git history shows the button predates IconButton (e5a5a4f, 2026-01-20: `bg-white rounded-full p-1 text-red-500 ... group-hover:opacity-100`) and was then patched IN PLACE three times — token migration (b6c57c3), a11y 44px+focus ring (6631c03), ring-token fix (0fd9037) — instead of being migrated, i.e. classic legacy drift, not a choice. (2) No DECISION comment or ADR justifies the divergence; the sibling raw photo-remove buttons in CompletionPhotoPicker.jsx:61-69 and TaskTimeLimitPopup.jsx:203-211 DO carry a comment explaining a deliberate small-badge + pseudo-element hit area, which this instance does not use — it renders a full 44px disc, exactly what IconButton renders. (3) Every bespoke detail is passable via IconButton's `className`: `absolute top-1 right-1 rounded-full bg-surface-card shadow`; precedent for `absolute top-2 right-2` overlay IconButtons exists in ManagerNotifications.jsx (1217, 1476, 1549, 1863). The one implementation caveat: IconButton merges with clsx (not twMerge) and hard-codes `rounded-control`, so `rounded-full` relies on cascade order — verified in the built dist CSS that `.rounded-full` (offset 18737) follows `.rounded-control` (18702), so the override takes effect. DESIGN_SYSTEM §8 (\"IconButton ... Replaces all p-1.5/p-0.5 icon buttons\") and CLAUDE.md \"Canonical components only\" make this a genuine discipline violation. Note: there is NO functional/a11y defect — the raw button already meets 44px, has an accessible name and a visible focus ring — so severity discipline/medium (not a correctness bug) is the right framing.",
        "Confirmed drift, not a design choice. Read src/components/TaskModal.jsx:1996-2003: a raw <button> reproducing IconButton's base contract verbatim (inline-flex items-center justify-center min-h-touch min-w-touch + the full focus-visible ring set) plus overlay styling (absolute top-1 right-1 bg-surface-card rounded-full shadow, danger colours). The project rule is explicit: DESIGN_SYSTEM §7 \"Use the IconButton component (§8) which enforces this\", §8 \"Replaces all p-1.5/p-0.5 icon buttons\", §11 checklist \"Reused the canonical component instead of a new bespoke shell\", CLAUDE.md \"Canonical components only\". Chesterton's fence checked and found empty: (1) IconButton already accepted className/children/variant='danger' at birth (0305cdd, 2026-06-20), so nothing forced the hand-roll; (2) the exact composition is shipped elsewhere — <IconButton variant=\"danger\" className=\"rounded-full\"> at ProfilePage.jsx:553 and absolute-positioned IconButton overlays in CallTimer:154, QuickWorkTimer:218, ManagerNotifications ×4, QuickWorkDescribePrompt:162; the built CSS emits .rounded-full after .rounded-control so the radius override wins under IconButton's plain clsx; (3) the commit that hand-rolled this touch target (3af4762, same day) converted three sibling raw buttons in this same file to IconButton and left this one raw with no reason in the body and no inline comment; (4) the only documented rationale for a raw remove-photo button (TaskTimeLimitPopup.jsx:197-201: off-centre 20px glyph with the 44px hit area spanning inward, which IconButton's centering cannot do) does not transfer — TaskModal's button IS a centred 44px visible circle, exactly IconButton's geometry; (5) no ADR, decisions-log entry, audit do-not-re-flag register entry, or 2026-07-31 a11y-audit note carves it out. The duplicate-contract cost has already been paid once: 0fd9037 had to hand-patch ring-brand → ring-brand-ring on this button separately while IconButton received the fix once. Severity discipline/medium is fair (no behaviour bug); the sibling raw button at lines 2019-2026 is the same drift.",
        "Confirmed against source. src/components/TaskModal.jsx:1996-2003 is a raw <button> whose className reproduces IconButton's base contract verbatim (\"inline-flex items-center justify-center min-h-touch min-w-touch\" + the exact \"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-ring focus-visible:ring-offset-2\" triple) plus the `danger` variant's \"hover:bg-feedback-danger-soft\". IconButton is imported at line 29 and used at 1518, 2072, 2229, 2237, 2243, 2253 in the same file, so the claim's premise holds. The design system makes IconButton mandatory here: DESIGN_SYSTEM.md §7 (l.175-177) \"Use the IconButton component (§8) which enforces this\", §8 (l.209-211) \"Replaces all p-1.5/p-0.5 icon buttons\", §11 checklist (l.434) \"Reused the canonical component (§8) instead of a new bespoke shell\"; IconButton.jsx's own header calls it \"the canonical icon-only control (DESIGN_SYSTEM §8)\". Chesterton's fence checked and no fence found: there is no comment or DECISION marker justifying the raw button (the adjacent comment only explains object-contain and the ZoomIn badge), and every extra the overlay needs (absolute top-1 right-1, bg-surface-card, rounded-full, shadow) is passable via IconButton's `className` without conflict — clsx appends className last, the `danger` variant has no base background to collide with, and the built CSS (dist/assets/index-*.css) emits `.rounded-control{8px}` BEFORE `.rounded-full{9999px}`, so rounded-full wins. The repo already does exactly this in src/pages/ProfilePage.jsx:548-555 (<IconButton variant=\"danger\" className=\"rounded-full\" .../> with the comment \"Canonical IconButton, not a 28px ad-hoc one\"), proving IconButton can express this control faithfully. Git history shows drift rather than a deliberate choice: the raw button predates IconButton (initial commit 2025-11; IconButton added 0305cdd 2026-06-20), and it was then hand-patched after IconButton existed — 0fd9037 (2026-07-31) changed only `focus-visible:ring-brand` -> `focus-visible:ring-brand-ring` on this line, i.e. the same ring IconButton already carried was re-applied by hand in a second place. The raw button also silently lacks IconButton's `active:scale-95` press feedback (§8 l.489) and `disabled` styling. Minor honest deltas (w-4 vs w-5 glyph; `text-feedback-danger` vs the variant's `text-feedback-danger-text`) are cosmetic and do not justify a bespoke control. Note the sibling raw remove button at lines 2019-2026 has the identical issue but is outside this claim's scope. Severity discipline/medium is appropriate."
      ]
    },
    {
      "title": "Break-time totals drift off the SESSION_COLORS map (feedback.warning instead of session.break)",
      "file": "src/components/DailyStatistics.jsx",
      "line": "1427-1429",
      "severity": "medium",
      "detail": "The desktop toolbar's 'Pertraukos' (break) hero total renders with `text-feedback-warning`/`text-feedback-warning` (Coffee icon + h2 figure, lines 1427 & 1429) — a `feedback.*` token (amber-500 / `#F59E0B`), not `session.break.accent` (amber-700 / `#B45309`) from the single SESSION_COLORS map in src/utils/sessionColors.js. This is exactly the drift DESIGN_SYSTEM §4-B calls out as a bug class ('today the call state is blue in some places and sky in others'): the SAME metric, in the SAME file, is rendered correctly with `text-session-break-accent` a few hundred lines away (DailyStatistics.jsx:1529, the mobile card variant of the identical 'Pertraukos' total). The same substitution (`text-feedback-warning-text` / `bg-feedback-warning-soft` in place of the session-break tokens) repeats for break-duration figures at lines 1443, 1581, 1603, 1665, 1687, 1750, 1792 and 2421/2521 in the same file, while every other session-color consumer in the app (BreakTimer.jsx, CombinedHoursSummary.jsx:387, DailyWorkProgress.jsx, Reports.jsx, SessionTypeIcon.jsx) reads session.break off the shared map. Because `feedback.warning` and `session.break.accent` are independent CSS-variable sets (`--fb-warning*` vs `--session-break-*` in src/index.css), a future palette tweak to either token is not guaranteed to keep the two in sync, and in the one case that does differ today (line 1427/1429 uses the amber-500 `feedback.warning` DEFAULT rather than the amber-700 `text` variant) the break total literally renders a different amber shade than the rest of the app's break UI.",
      "dimension": "session-color",
      "votesReal": 3,
      "votesTotal": 3,
      "confirmed": true,
      "reasons": [
        "CONFIRMED after reading src/components/DailyStatistics.jsx and the surrounding system. Line 1427 (Coffee icon) and 1429 (text-h2 hero figure) render `totalBreakMinutes` with `text-feedback-warning` — the fill-tuned DEFAULT (amber-500 #F59E0B light) — while line 1529, the mobile card for the IDENTICAL metric in the same file, uses `text-session-break-accent` (amber-700 #B45309). Line 1665 (per-user table break cell) also uses the DEFAULT; 1581/1603/1688/1750/1792/2421/2521 use `text-feedback-warning-text`. Not a deliberate choice: git blame traces the toolbar lines to 3fff798d (2026-06-23 theme migration), whose diff shows a mechanical nearest-token swap `text-amber-600` -> `text-feedback-warning` as part of \"~150 raw palette utilities migrated\"; the commit body says nothing about break semantics, and no ADR, doc, or code comment anywhere justifies `feedback.warning` for break figures (tokens.md/tailwind define it as \"warnings\" / \"messages & validation — never decorative\"). The convention is documented the other way: SESSION_COLORS's own header names §4-B drift as \"the bug this map removes\"; Reports.jsx:1216 explicitly states \"break = session-break accent\" for the same Veikla/Pertraukos/Viso triplet that was moved OUT of DailyStatistics; and every other break-figure consumer (BreakTimer, CombinedHoursSummary, DailyWorkProgress, Reports, StatRow, sessionToggleClasses) reads session.break — DailyStatistics is the sole outlier (grep confirms no other component uses feedback-warning for a break value). The two token sets are independent CSS-variable families (--fb-warning* vs --session-break-*) with separate dark overrides (index.css:370 vs :380), so nothing keeps them coupled. Visible consequence today: in light mode the desktop hero and table cell render amber-500 vs the app-wide amber-700, and amber-500 on the white card is ~2.15:1, below the §7 3:1 large-text floor for the 20px bold figure and the 4.5:1 normal-text floor for the 16px table cell — so the DEFAULT-as-text substitution is also a live AA contrast fail, which the mobile counterpart does not have. A prior audit (2026-06-27 #9) already confirmed the same \"Rule B drift\" class for TaskTimeWarningPopup; no audit accepted this instance as design. Two precision nits in the claim that do not change the verdict: line 1443 is the anomaly role=\"alert\" banner, a genuine warning correctly on feedback.warning and should be excluded from the list; and \"1687\" is actually 1688. The `-text` variants are pixel-identical to session-break-accent in both themes today, so those sites are latent semantic drift rather than a visible difference — consistent with the medium severity.",
        "CONFIRMED after reading the source. All cited lines are accurate: src/components/DailyStatistics.jsx:1427 (Coffee icon) and :1429 (h2 hero total) use `text-feedback-warning` (the DEFAULT fill variant, amber-500 `--fb-warning: 245 158 11`), while the identical 'Pertraukos' total on the mobile card at :1529 uses `text-session-break-accent` (amber-700 `--session-break-accent: 180 83 9`). Lines 1443/1581/1603/1665/1687/1750/1792/1817/2421/2521 likewise use `feedback-warning*` for break figures.\n\nChesterton's fence checked and NOT found: `git blame` shows the feedback-warning classes were introduced by the theme commit 3fff798 (2026-06-23, \"user-selectable light/dark/system theme\") as a mechanical literal→token sweep — `text-amber-600` → `text-feedback-warning`, `text-amber-700` → `text-feedback-warning-text` — with no break-semantics reasoning; b44cf75 the same day only enlarged the font and kept the token. The mobile card (92bfd06, a day earlier) was written directly on `text-session-break-accent`. No ADR, decisions-log entry, DESIGN_SYSTEM/tokens.md text, or code comment records an intent to color break durations with a feedback token. To the contrary, tailwind.config.js documents `feedback` as \"messages & validation — never decorative\" and its DEFAULT as \"solid fill / on-white foreground icon (pairs with text-white)\", not as text; and every other break-duration consumer reads the SESSION_COLORS break accent: BreakTimer.jsx:313, CombinedHoursSummary.jsx:387, DailyWorkProgress.jsx:300/329, Reports.jsx:1039/1226 — Reports.jsx:1216 even states the convention explicitly (\"break = session-break accent, total = brand\"). So DailyStatistics is the lone outlier, and inconsistent with itself.\n\nThe drift is visible today, not only latent: `--surface-card` is #FFFFFF in light mode, and the h2 (20px bold) hero total in amber-500 on white measures 2.15:1 — failing even the 3:1 large-text floor (the original literal amber-600 was 3.19:1; session-break-accent is 5.02:1, AA). The `-text` variants elsewhere in the file coincide with session-break-accent today (amber-700 light / amber-400 dark), so those sites are latent-drift only; lines 1427/1429 are a real shade mismatch plus a contrast regression introduced by the mechanical mapping. Medium severity is appropriate; the fix is a class swap to `text-session-break-accent` (or SESSION_COLORS.break.accent) at the listed sites.",
        "CONFIRMED by reading src/components/DailyStatistics.jsx:1427-1429. The desktop toolbar 'Pertraukos' hero renders the Coffee icon and the h2 total with `text-feedback-warning` (the DEFAULT triad member = amber-500 #F59E0B via --fb-warning) while the identical mobile-card total at line 1529 uses `text-session-break-accent` (amber-700 #B45309 via --session-break-accent). Both sit in the same file, same metric (totalBreakMinutes). Chesterton's-fence check finds no deliberate reason: git blame shows line 1427 came from the theme sweep 3fff798 (2026-06-23) as a mechanical `text-amber-600` -> `text-feedback-warning` swap; b44cf75 only enlarged text-body->text-h2; neither commit body, ADR 0016, nor any inline DECISION comment records choosing feedback over session for break durations, and the sibling commit 92bfd06 a day earlier chose session-break-accent for the same figure. It also breaks the token contract even within the feedback family: tailwind.config.js documents DEFAULT as 'solid fill / on-white foreground icon (pairs with text-white)' and `text` as 'accent text on a tint/white (AA on soft)'. App-wide survey: every other break-figure consumer reads session.break (BreakTimer.jsx:313, DailyWorkProgress.jsx:300, Reports.jsx:1039/1226, ActiveWorkSessions.jsx:171, sessionToggleClasses.js:46, CombinedHoursSummary.jsx:387 bg-session-break-accent); zero feedback-warning usages tied to break exist outside DailyStatistics. Reproducible visible consequence, not just hygiene: the toolbar is on bg-surface-card (#FFFFFF, line ~1358); measured contrast amber-500 on white = 2.15:1 (2.06 on base, 1.95 on sunken), failing WCAG AA even at the 3:1 large-text threshold that the 20px-bold h2 qualifies for, and the 3:1 non-text threshold for the icon; amber-700 would give 5.02:1. DESIGN_SYSTEM §7 makes AA mandatory and §4-B names this exact drift class. Line 1665 (team table break cell) has the same DEFAULT-as-text-on-white problem. Caveats that trim but do not defeat the claim: in dark mode both classes are overridden to amber-400 (index.css:370/380) so they coincide there; the other cited lines using `-text`/`-soft` (1581, 1603, 1687, 1750, 1792, 2421, 2521) resolve to the same amber-700/amber-400 values as session.break today, so they are latent drift only; and line 1443 is a genuine warning banner, a legitimate feedback.warning use the claim over-includes. No test locks this (only SessionToggleButton.test.js touches these tokens). Medium severity is appropriate."
      ]
    },
    {
      "title": "Date-range popover trigger button is under the 44px touch-target floor",
      "file": "src/components/TaskHistory.jsx",
      "line": "35-37, 804-814",
      "severity": "medium",
      "detail": "SELECT_CLASS ('bg-surface-card border border-line text-ink text-body rounded-input block w-full px-2.5 py-1.5 ...') has no min-h-touch/min-h-[44px] utility, and the date-range trigger button at line ~809 (`className={clsx(SELECT_CLASS, 'flex items-center gap-2 text-left')}`) uses only this class. With text-body (14px/20px line-height per tailwind.config.js) plus 1px border and py-1.5 (12px) padding, the rendered hit area is roughly 32-34px tall — well under the DESIGN_SYSTEM §7 44px floor. This is the sole control that opens the Nuo/Iki date pickers for both the personal and team history views, on every viewport (the filter row is not gated by md:hidden). Every other hand-rolled trigger in the app that shares this padding shape (Reports.jsx L1270, stats/StatPeriodPicker.jsx L43, reports/PeriodPicker.jsx L93) explicitly pairs it with min-h-touch, so this is an isolated regression, not house style.",
      "dimension": "ux-a11y",
      "votesReal": 2,
      "votesTotal": 2,
      "confirmed": true,
      "reasons": [
        "Verified in src/components/TaskHistory.jsx (worktree). L35-37: SELECT_CLASS = 'bg-surface-card border border-line text-ink text-body rounded-input block w-full px-2.5 py-1.5 focus:...' — no min-h-touch / min-h-[44px]. L809: the date-range trigger button uses only clsx(SELECT_CLASS, 'flex items-center gap-2 text-left'). tailwind.config.js: text-body = 14px/20px, minHeight.touch = 44px. Rendered height = 20 (line-height) + 12 (py-1.5) + 2 (border) = 34px, under the DESIGN_SYSTEM §7 floor (\"≥ 44 × 44 px hit area for every interactive control\"), which §1.2 explicitly refuses to trade even for manager density. Not a Chesterton's fence: git blame shows SELECT_CLASS was authored 2026-06-20 (921b173) for the then-native <select> filter controls; commit e5b0072 later replaced every native <select> with the canonical ui/Select whose trigger (Select.jsx L284) carries min-h-touch — SELECT_CLASS survived only on this one button as a leftover, with no comment or commit justifying a compact height. The same file already fixed its accordion header with min-h-touch citing §7 (L721-723), and PeriodPicker.jsx L93 comments \"min-h-touch, not h-10: a 40px control misses the binding 44px gate\" — so house style is 44px triggers and this is an isolated miss. Exposure: the filter row (L782) has no md:/hidden gate and the date-range block is not behind !isPersonal, so it renders for worker personal history on phones too; it is the sole path to the Nuo/Iki DatePickers. One nit in the claim: Reports.jsx L1270 is a warnings drill-down button, not a period trigger, but it does carry min-h-touch so the comparison still holds.",
        "Confirmed against source. src/components/TaskHistory.jsx:35-37 defines SELECT_CLASS with `text-body` (tailwind.config.js:141 = 14px/20px line-height), `py-1.5` (6px x2) and `border` (1px x2), and no min-h utility; the date-range trigger at L804-814 adds only `flex items-center gap-2 text-left`. Rendered box = 20 + 12 + 2 = 34px, and src/index.css has no global button min-height (its only min-height rule is the rbc timeslot at L437), so the hit area is ~34px vs the binding >=44px floor in docs/design/DESIGN_SYSTEM.md §7 (L175-178) and the §11 checklist (L427). The `<label>` at L802 has no htmlFor, so the label does not extend the target. Viewport: the filter row at L782 is `flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-end`, gated only by `historyOpen`, not by any md:/hidden wrapper — it renders on phones for both personal and team history, and this button is the only way to reach the Nuo/Iki DatePickers. Chesterton's fence checked: SELECT_CLASS was authored in 921b173 for native `<select>` elements (hence the name); those were later replaced by the canonical Select (e5b0072) and DatePicker (911fcf1), and the button (introduced in f99a9a7) simply reused the leftover class as a look-alike — it is now SELECT_CLASS's sole consumer. No comment or commit message justifies a sub-44 control; the opposite pattern is in force everywhere around it: the same file's header toggle (L721-723) carries an explicit \"min-h-touch ... (§7)\" comment, its row siblings SearchBox (ui/SearchBox.jsx:174) and DatePicker trigger (ui/DatePicker.jsx:219) are min-h-touch, the reset IconButton is 44, and the cited comparators (Reports.jsx L1270, stats/StatPeriodPicker.jsx L43, reports/PeriodPicker.jsx L93 — the latter with the comment \"a 40px control misses the binding 44px gate\") all pair py-1.5 with min-h-touch. Isolated regression, not house style; medium severity is fair (a11y gate violation on a real, mobile-visible control, easy one-token fix)."
      ]
    },
    {
      "title": "Rūšiuoti (sort) segmented-switch buttons are under the 44px touch-target floor",
      "file": "src/components/TaskHistory.jsx",
      "line": "916-940",
      "severity": "medium",
      "detail": "The 'Pagal datą' / 'Pagal būseną' segmented toggle buttons use only `px-3 py-1.5 text-body font-semibold` with no min-h-touch, giving each button a ~32px hit area (12px vertical padding + 20px line-height) versus the required ≥44px. This control is rendered whenever `historyOpen && !isPersonal`, i.e. on the manager team view on every viewport including phones, alongside otherwise properly-sized filter controls (Select, DatePicker, IconButton) in the same toolbar.",
      "dimension": "ux-a11y",
      "votesReal": 2,
      "votesTotal": 2,
      "confirmed": true,
      "reasons": [
        "Confirmed against source. src/components/TaskHistory.jsx:916-940 — both segmented buttons carry only \"px-3 py-1.5 text-body font-semibold ...\" with no min-h-touch. tailwind.config.js:141 defines `body: ['14px','20px']`, so the button box is 20px line-height + 2×6px padding = ~32px tall (the 1px border is on the wrapper div, not the button) — below the mandatory ≥44×44 floor in docs/design/DESIGN_SYSTEM.md §7, and §1 principle 2 says the 44px target is never traded for density. Rendering path: the toolbar wrapper (line 782, `flex flex-col gap-3 sm:flex-row ...`) has no `hidden md:` gate, and `isPersonal` (line 125) is false for any manager/admin viewing 'all', so the control does appear on phones. Chesterton's-fence check rules out a deliberate exemption: the same commit f99a9a7 that introduced this switch also created the identical WorkPlanner Savaitė/Diena segmented switch WITH `min-h-touch` (git show f99a9a7 -- WorkPlanner.jsx shows \"w-[84px] sm:w-[100px] min-h-touch text-body font-semibold\"), and every other aria-pressed segmented/pill control in the codebase (StatPeriodPicker.jsx:43, TaskModal.jsx:1582/1590 templateScope, ui/FilterPills.jsx:19, ui/Select.jsx:284 trigger) carries `min-h-touch` — the `minHeight.touch: '44px'` token (tailwind.config.js:162) exists for exactly this. The commit's \"tighter filter layout (§9 dual density)\" reason covers denser manager *tables* on md+, not sub-44px controls, and the design system does not exempt manager toolbars. Minor correction to the claim's framing: the adjacent date-range trigger button (line 804-815, SELECT_CLASS with py-1.5) is also ~32px, so not every neighbouring control is properly sized — but the ui/Select component in the same toolbar is (min-h-touch), and that does not weaken the core finding. Size figure is computed from tokens, not measured in a live render.",
        "Confirmed from source. src/components/TaskHistory.jsx:916-940 — both segmented buttons carry only `px-3 py-1.5 text-body font-semibold …` with no `min-h-touch`/`min-h-*`. tailwind.config resolves `text-body` to 14px/20px, so the hit area is 20 + 2×6 = 32px, well under the 44px floor (DESIGN_SYSTEM §7 \"≥ 44 × 44 px hit area for every interactive control\"; §11 checklist). The toolbar wrapper (line ~781, `flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:items-end`) has no `hidden md:` gate, so the switch renders on phones whenever `historyOpen && !isPersonal` (manager team view).\n\nChesterton's fence checked and does not hold: the introducing commit f99a9a7 states its intent as \"Reduce vertical bulk on phones … (DESIGN_SYSTEM §9 dual density)\", but §9's density relaxation is explicitly for *tables on md+/desktop*, and DS Principle 2 says \"We never trade the 12 px floor (§5) or the 44 px touch target (§7) for fitting more on screen.\" Compactness was the motive, but the design system forbids that exact trade for touch targets. No DECISION comment or exception exists near the switch.\n\nIt is also an outlier, not a house pattern: every sibling segmented/aria-pressed toggle honours the floor — StatPeriodPicker.jsx:43 (`min-h-touch`), TaskModal.jsx:1583/1591 templateScope switch (`min-h-touch`), ui/FilterPills.jsx:19 (`min-h-touch`, comment cites the 44px rule), TaskFlagToggles.jsx (doc says ≥44px). The `min-h-touch` token exists (tailwind.config:162) and is used in 46 files; the `Select` sitting in the same toolbar uses it (ui/Select.jsx:284). One-line fix: add `min-h-touch` to both button class lists.\n\nMinor correction to the claim's framing only: the adjacent date-range button (SELECT_CLASS, `px-2.5 py-1.5`, no min-h-touch) is likely also under 44px, so \"alongside otherwise properly-sized controls\" is slightly overstated for DatePicker trigger — but that does not affect the validity of this finding."
      ]
    },
    {
      "title": "Bespoke icon-only 'more time options' button duplicates IconButton, same file",
      "file": "src/components/TaskModal.jsx",
      "line": "1906-1915",
      "severity": "low",
      "detail": "Another raw `<button>` (the `Plus`/\"Daugiau…\" trigger next to the estimated-time chips) reimplements IconButton's touch-target/focus-ring markup by hand rather than calling the already-imported `IconButton` used elsewhere in this file. Functionally fine, but it is the same canonical-component bypass as the attachment-remove button above, showing the drift is not a one-off.",
      "dimension": "discipline",
      "votesReal": 3,
      "votesTotal": 3,
      "confirmed": true,
      "reasons": [
        "Confirmed against source. src/components/TaskModal.jsx:1906-1915 is a raw <button> (icon-only Plus, aria-label, title=\"Daugiau…\", disabled={fieldsLocked}) whose className hand-rolls exactly what IconButton (src/components/ui/IconButton.jsx) guarantees: inline-flex + min-h-touch/min-w-touch, focus-visible:ring-2 ring-brand-ring, disabled:opacity-50, 20px icon. IconButton is imported at line 29 and used 6 times in the same file (1518, 2072, 2229-2253), so this is a genuine canonical-component bypass (DESIGN_SYSTEM §8), not a missing dependency. Chesterton's-fence check found no deliberate reason: (1) the only plausible one — the pill shape (rounded-full + border-line to match the neighbouring chips) — is achievable via IconButton's className: I generated the Tailwind CSS and .rounded-full is emitted at line 1458, AFTER .rounded-control at 1454, so it wins; the repo already does this at src/pages/ProfilePage.jsx:547-554 (IconButton className=\"rounded-full\" beside chips). (2) OneLineChips (lines 117-165) wraps `more` in its own measured <span ref={moreRef}>, imposing no ref/shape constraint on the child. (3) Introducing commit 6b4f35dc (2026-06-24) states no reason for a raw button; 0fd90379 (a11y audit) only re-tokened its ring class without converting it; no justifying code comment exists. (4) IconButton's API covers every prop used (icon, label, title separate from label, onClick, disabled, className, ...rest). The \"not a one-off\" framing is also accurate: the attachment-remove button at 1996-2003 and a third at 2019-2026 are raw icon-only <button>s with the same hand-rolled touch/ring markup. Functionally fine (targets and rings are present), hence low/discipline severity — but the issue is real and not a design choice.",
        "Confirmed against src/components/TaskModal.jsx:1906-1915 (worktree checkout). The \"+\"/\"Daugiau…\" trigger passed as OneLineChips' `more` prop is a raw <button> whose className hand-types the exact IconButton base recipe — `inline-flex min-h-touch min-w-touch items-center justify-center … focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-ring disabled:opacity-50` — while `IconButton` is imported at line 29 and used six times in the same file, once with the very same `Plus` icon (line 2072). IconButton (src/components/ui/IconButton.jsx) already supports every prop this button needs: `label` (aria-label), `title`, `disabled`, `onClick`, `className` merge, and `...rest`. Chesterton's-fence check: the only visual deltas are `rounded-full` + `border border-line` so the \"+\" reads as a peer of the pill chips; both are reachable via `className` — ProfilePage.jsx:553 already does `<IconButton … className=\"rounded-full\">`, and in the built CSS `.rounded-full` (offset 18737) is emitted after `.rounded-control` (18702), so the override wins under IconButton's plain clsx. OneLineChips only reads `moreRef.offsetWidth`, which a 44px IconButton yields identically, so the measurement logic imposes no constraint. No comment in the code, in the introducing commit 6b4f35dc (2026-06-24 declutter), or in the 0fd90379 a11y pass gives a reason to bypass IconButton; the a11y commit just re-pointed the ring token on this bespoke string instead of consolidating. DESIGN_SYSTEM §2 rule 3 (\"one way to do a thing\") and §8 (\"IconButton … replaces all icon buttons\") make this a genuine canonical-component bypass. The adjacent assertion that it is not a one-off also holds: grep shows two more raw icon-only `min-w-touch` buttons in the file (attachment remove at 2000, checklist-item remove at 2023). Severity as stated — discipline/low, functionally fine (44px target, focus ring, aria-label all present) — is accurate; the only subtle behavioural change a consolidation would bring is IconButton's `ring-offset-2`, `active:scale-95` and `disabled:pointer-events-none`, which §12 explicitly wants on icon buttons.",
        "Confirmed by reading src/components/TaskModal.jsx:1906-1915: a raw <button> hand-rolls IconButton's exact contract (inline-flex min-h-touch min-w-touch, focus-visible ring-brand-ring, disabled:opacity-50, aria-label, title, 20px lucide Plus child with aria-hidden) while IconButton is imported at line 29 and used 7x in the same file, including with the same Plus icon at line 2072. Chesterton's fence checked: the only plausible reason (chip-row rounded-full + border-line look) is achievable via IconButton's className prop — src/pages/ProfilePage.jsx:548-555 already does exactly that inside a chip and the built CSS emits .rounded-full (offset 18737) after .rounded-control (18702) so the override wins; OneLineChips only measures the more-slot's offsetWidth, which a 44px IconButton matches. No DECISION comment, no audit-register acceptance, and the introducing commit 6b4f35dc gives no rationale. The comparison to the attachment-remove raw buttons (lines 1996-2003, 2018-2025) is also accurate. Severity is correctly low: no functional or WCAG defect, only a DESIGN_SYSTEM §3/§11 canonical-component discipline drift."
      ]
    },
    {
      "title": "Previously-known 'sessions' collection rule gap is already fixed — not a live issue",
      "file": "firestore.rules",
      "line": "848-855",
      "severity": "low",
      "detail": "The 'known live example' cited in the task (sessions written in sessionActions.js with no matching rule, silently default-denied) is no longer accurate for this checkout. firestore.rules:848-855 now has match /sessions/{sessionId} with allow create: if isUserActive() && createOwnsUserId() (create-only; read is manager/admin, update/delete are false), and its own comment states it was added specifically because the collection 'Previously had NO rule, so every write was silently default-denied'. The client write at src/utils/sessionActions.js:460 (addDoc(collection(db,'sessions'), { userId, ... })) stamps userId on the payload, which satisfies createOwnsUserId() (request.resource.data.userId == request.auth.uid) at firestore.rules:149-151, so the write now succeeds. The write is also no longer fully silent on failure: it's chained with .catch(err => logError(err, { source: 'writeFail:endSession.sessionLog' })) (sessionActions.js:461-465), so any future rejection would at least reach the error_logs sink/local ring buffer instead of vanishing outright. Flagging this only so the parent doesn't re-report a already-patched gap; no action needed.",
      "dimension": "firebase-coupling",
      "votesReal": 2,
      "votesTotal": 3,
      "confirmed": true,
      "reasons": [
        "The claim is accurate: the 'sessions' rule gap is already fixed in this checkout and is not a live issue. Verified in source: firestore.rules:851-855 has `match /sessions/{sessionId}` with `allow create: if isUserActive() && createOwnsUserId()`, `allow read: if isManagerOrAdmin()`, `allow update, delete: if false`, and the comment at 849-850 explicitly cites the former default-deny gap; git history shows the rule landed 2026-06-19 (8fab4a0) and was tightened 2026-06-21 (4ed91be), so it is long-standing, not a fresh patch. createOwnsUserId() at firestore.rules:149-151 is `request.resource.data.userId == request.auth.uid`. The only client write to `sessions` (src/utils/sessionActions.js:460-469) stamps `userId` on the payload and is chained with `.catch(err => logError(err, { source: 'writeFail:endSession.sessionLog' }))` (the claim's cited 461-465 is off by a few lines; the catch is at 469 — immaterial). Owner-match holds for every caller: BreakTimer.jsx:251, CallTimer.jsx:452, QuickWorkTimer.jsx:604/677 pass `currentUser.uid`, and useOrphanedSessionRecovery.js:161/197 derives `uid` from `currentUser.uid`; there is no manager-ends-another-user's-session path that would make userId != request.auth.uid. isUserActive() (rules:26-30) only requires an existing non-disabled user doc, the same precondition as every other worker write. Caveat: this verifies the checkout's rules file, not the live deployed ruleset (Firebase MCP timed out), but the claim is explicitly scoped to this checkout. Net: the parent should NOT re-report the 'sessions written with no matching rule' gap; no action needed.",
        "Not a live issue — the finding is itself a \"no action needed\" note, and the source confirms it. firestore.rules:851-855 has `match /sessions/{sessionId}` with `allow create: if isUserActive() && createOwnsUserId()`, `allow read: if isManagerOrAdmin()`, `allow update, delete: if false`; the block's comment (848-850) explicitly says it was added because the collection previously had no rule and writes were default-denied. `createOwnsUserId()` (firestore.rules:149-151) checks `request.resource.data.userId == request.auth.uid`, and the client write at src/utils/sessionActions.js:460-470 stamps `userId` on the payload. Every production caller passes the signed-in user's own uid (BreakTimer.jsx:251, CallTimer.jsx:452, QuickWorkTimer.jsx:604/677 use `currentUser.uid`; useOrphanedSessionRecovery.js:197 passes its own `uid`), so the owner pin is satisfied and the create succeeds. The write is also chained with `.catch(err => logError(err, { source: 'writeFail:endSession.sessionLog' }))` (line 469), so a future rejection would reach the error sink rather than vanish. The \"known live example\" of a rule gap is therefore stale for this checkout; there is no defect to report and nothing to fix. Correctly classified as informational only — should not surface as a finding.",
        "Verified against source in the worktree (C:\\Users\\karol\\Desktop\\WORKZ\\.claude\\worktrees\\gallant-moser-71646e). The claim is accurate: the old \"sessions has no rule -> default-denied\" gap is closed in this checkout.\n\nEvidence:\n1. firestore.rules:848-855 contains `match /sessions/{sessionId}` with `allow create: if isUserActive() && createOwnsUserId();`, `allow read: if isManagerOrAdmin();`, `allow update, delete: if false;`. The comment at lines 849-850 explicitly states it was added because the collection \"Previously had NO rule, so every write was silently default-denied\".\n2. `createOwnsUserId()` (firestore.rules:149-151) is `request.resource.data.userId == request.auth.uid`. `isUserActive()` (line 26) and `isManagerOrAdmin()` (line 52) both exist.\n3. The client write at src/utils/sessionActions.js:460-469 is `addDoc(collection(db, 'sessions'), { userId, userName, type, startTime, endTime, durationMinutes, date, metadata })`. `userId` is the first parameter of `endSession` (line 330), and every production caller passes the signed-in uid (BreakTimer.jsx:251, CallTimer.jsx:452, QuickWorkTimer.jsx:604/677 pass `currentUser.uid`; useOrphanedSessionRecovery.js:197 passes the recovered `uid`). So the payload satisfies the create rule; the rule imposes no other shape constraints that the payload could violate.\n4. The write is chained with `.catch(err => logError(err, { source: 'writeFail:endSession.sessionLog' }))` (actual line 469, not 461-465 as claimed — immaterial), and `logError` is imported at sessionActions.js:6, so a future rejection would reach the error sink rather than vanish.\n5. Provenance: the rule block was introduced in commit 4ed91be \"fix(security): scope Firestore writes per-owner with manager escape\", which is on origin/main — it is merged code, not a worktree-only artifact.\n\nChesterton's fence: not applicable — this finding does not condemn anything; it correctly records that a previously-known gap is already patched so it is not re-reported. Scope caveat: this confirms the REPO checkout only. Whether the LIVE deployed ruleset carries this rule is a separate deploy-state question (the Firebase MCP timed out this session, so live state was not checked), but the claim is explicitly scoped to \"this checkout\", which is accurate. No action needed."
      ]
    }
  ],
  "rejected": [
    {
      "title": "Bespoke clear-search icon button duplicates IconButton despite it being imported/used in this file",
      "file": "src/components/UserManagement.jsx",
      "line": "1173-1180",
      "severity": "medium",
      "detail": "The roster search field's clear ('X') control is a raw `<button>` with `min-h-touch min-w-touch ... rounded-full ... focus-visible:ring-2 focus-visible:ring-brand-ring` hand-copied from IconButton, instead of `<IconButton icon={X} label=\"Išvalyti paiešką\" onClick={...} />`. `IconButton` is imported (line 19) and used elsewhere in the same file (lines 520, 552, 584), so this is a concrete inconsistency rather than a case IconButton can't cover.",
      "dimension": "discipline",
      "votesReal": 0,
      "votesTotal": 3,
      "confirmed": false,
      "reasons": [
        "Mechanically accurate but not a real defect — it is a deliberate codebase convention. The raw clear-X at src/components/UserManagement.jsx:1173-1180 matches how the canonical ui/ components build the same control: src/components/ui/SearchBox.jsx:179-190 (the canonical search field) and src/components/ui/DatePicker.jsx:233-250 both render an inside-the-input clear button as a raw <button>, and DatePicker does so while importing IconButton (line 17) and using it for its nav arrows (282, 290) — clear evidence the choice is intentional. Technical reason: IconButton composes with plain clsx (not tailwind-merge), so its baked-in focus-visible:ring-offset-2, rounded-control, active:scale-95 and fixed 20px icon cannot be removed via className; a ring offset on a control drawn inside an input's bounds paints over the input's border, which is why all three inline clear controls omit it and use a 16px glyph. IconButton's stated purpose (its doc block + DESIGN_SYSTEM §8 line 211) is replacing undersized p-1.5/p-0.5 icon buttons; the roster button already meets the ≥44px target, aria-label and focus-ring guarantees, so the fence's intent is satisfied. The three IconButton usages in this file are freestanding toolbar controls, a different context. Converting it would make UserManagement inconsistent with SearchBox/DatePicker rather than more consistent.",
        "Read src/components/UserManagement.jsx:1158-1181 plus IconButton.jsx, ui/SearchBox.jsx, ui/DatePicker.jsx, utils/cn.js, DESIGN_SYSTEM §7/§8/§11, and the two authoring commits (1bf3233, 0fd9037). The claim's premise is wrong and the fence has a reason.\n\n1) Not \"hand-copied from IconButton\" — it mirrors the CANONICAL inline-clear pattern. ui/SearchBox.jsx (the canonical search field, created 259694b on 2026-06-23, one day before this roster commit) builds its own clear control as a raw <button> (absolute right-pinned, h-4 w-4 X, text-ink-muted hover:text-ink, brand focus ring with NO ring-offset) and carries an explicit rationale comment: a 44px box pinned inside the field's reserved right padding so it never overlaps typed text. ui/DatePicker.jsx:235-250 does the same for \"Išvalyti datą\". So \"raw <button> for a field-adornment clear\" is the house convention in the canonical library itself; UserManagement's control follows it class-for-class (right-1.5, -translate-y-1/2, h-4 w-4, no offset).\n\n2) IconButton cannot reproduce this control faithfully. It composes with plain clsx (not the repo's tailwind-merge cn — cn.js even registers the custom `rounded` group precisely because clsx can't resolve such conflicts), so a caller's `rounded-full` cannot reliably override its pinned `rounded-control` (8px). It also unconditionally adds `focus-visible:ring-offset-2` (an offset halo that paints over the field's border/text — both canonical inline clears deliberately omit the offset), `active:scale-95`, and a `title` tooltip; the `icon` prop forces w-5 h-5. Swapping would change shape, focus rendering and press behaviour inside the input, not merely tidy code.\n\n3) Design-system scope: §7/§8 position IconButton as the remedy for UNDERSIZED icon buttons (\"Replaces all p-1.5/p-0.5 icon buttons\"); this control already satisfies §7 (min-h-touch min-w-touch, aria-label, visible brand-ring). The 2026-07-31 WCAG AA remediation (0fd9037) edited exactly this className line (blame confirms) and kept it raw. The §11 checklist item is \"reused the canonical component instead of a new bespoke SHELL\", not \"no raw <button> anywhere\".\n\n4) Not a lone inconsistency: raw 44px touch-target buttons exist at ~14 sites across 7 files alongside 78 IconButton uses — a tolerated class wherever IconButton's fixed shape/behaviour doesn't fit (chips, segmented controls, field adornments). The three IconButton uses in this file (520/552/584) are standalone toolbar actions, a different role from an in-field adornment.\n\nResidual nit (not the claimed issue, not condemning): UserManagement lacks the rationale comment SearchBox has, so the choice reads as accidental; a one-line comment pointing at SearchBox's pattern would preempt this re-flag. Do not \"fix\" by swapping to IconButton.",
        "The observation reproduces (UserManagement.jsx:1173-1180 is a raw <button> with min-h-touch/min-w-touch, aria-label and the brand focus ring; IconButton is imported at line 19 and used at 520/552/584; IconButton's `ghost` variant is exactly `text-ink-muted hover:text-ink` and it accepts className/children), but it is not a defect. (1) IconButton's stated purpose (44px target + focus ring + accessible name, per its docstring) is already fully met by this button. (2) The correct peer group is \"clear control pinned inside an input\", and there the file is CONSISTENT with the codebase: the design system's own canonical ui/SearchBox.jsx:180-191 and ui/DatePicker.jsx:242-250 both hand-roll the identical raw in-field clear button (SearchBox with an explicit geometry comment); across 78 IconButton usages, zero are inside an input — the 520/552/584 uses cited are standalone toolbar buttons, a different placement class. (3) There is an evident fence: IconButton composes with plain clsx (not the project's cn/tailwind-merge), so a caller's rounded-full cannot override its baked-in rounded-control; it also forces focus-visible:ring-offset-2 (a halo over the input border), a fixed 20px glyph via `icon`, a `title` tooltip and active:scale-95 — all mismatched with the in-field affordance and with how ui/SearchBox's clear renders. (4) The proposed fix would make this clear DIVERGE from the reference SearchBox clear rendering, reducing consistency. The legitimate reuse question here is a different, larger one — UserManagement re-implements the whole search field instead of composing ui/SearchBox (which already exposes value/onChange/placeholder/ariaLabel) — and the claim's remedy would not address it."
      ]
    },
    {
      "title": "Bespoke icon-only remove-photo button and ghost text button duplicate IconButton/Button; neither is imported",
      "file": "src/components/TaskTimeLimitPopup.jsx",
      "line": "202-211, 273-281",
      "severity": "low",
      "detail": "The extension-request photo-remove control (202-211) reimplements IconButton's circular icon-button chrome by hand, and the 'Nutildyti garsą' mute control (273-281) reimplements Button's `ghost` variant by hand (`min-h-touch`, same focus-ring/disabled classes) — e.g. `<Button variant=\"ghost\" icon={BellOff} disabled={muted}>Nutildyti garsą</Button>` and `<IconButton icon={X} label=\"Pašalinti nuotrauką\" .../>` would cover both. Unlike the TaskModal.jsx instances, this file only imports `Button` (not `IconButton`), suggesting the icon-only case was never routed through the canonical component to begin with.",
      "dimension": "discipline",
      "votesReal": 1,
      "votesTotal": 3,
      "confirmed": false,
      "reasons": [
        "Read src/components/TaskTimeLimitPopup.jsx:197-211 and 272-282, plus ui/IconButton.jsx, ui/Button.jsx, DESIGN_SYSTEM §8, git blame, and the sibling galleries. The claim is a compound whose substantive half is refuted and whose remainder is a trivial, non-enforced style nit — and its headline is factually wrong (\"neither is imported\": Button IS imported at line 7 and used 5 times in the file).\n\n(1) Photo-remove control (202-211) is a deliberate, documented design that IconButton cannot express. The comment at 197-201 (commit 95e02be, a11y sweep) explains the layout: a full 44px hit box anchored `right-0 top-0` with `items-start justify-end` so the visible ~20px dark circle (`bg-black/60` wrapper around a 12px X) is pinned to the corner while the hit area spans inward over the photo, and `focus-visible:ring-inset` (added in a11y commit 0fd9037) because the parent tile is `overflow-hidden` — an offset ring would be clipped. IconButton hard-codes `items-center justify-center rounded-control ring-offset-2 active:scale-95` via clsx (no tailwind-merge), so a className override cannot win against Tailwind's fixed utility order, its icon slot renders a bare `w-5 h-5` glyph with no circle chrome, and passing the circle as `children` would centre it in the 44px box, not pin it to the corner. The suggested `<IconButton icon={X} label=.../>` would regress the shipped a11y fix (clipped focus ring) and lose the overlay affordance. The sibling gallery CompletionPhotoPicker.jsx:60-69 uses the same bespoke corner-badge pattern with its own documented rationale — this is the established pattern for on-thumbnail remove badges, not an oversight.\n\n(2) Mute control (273-281): technically a text+icon tertiary action rendered as a raw <button> while `Button variant=\"ghost\"` exists and is imported. Swapping is feasible with minor visual drift (Button forces font-semibold, w-5 icon, hover bg, active scale vs the bespoke gap-1.5 / h-4 icon / normal weight). But no ESLint rule bans raw buttons (.eslintrc.cjs has no such restriction), the repo has 111 raw <button> instances across 38 non-ui component files, and the amber sibling TaskTimeWarningPopup has no mute control to be inconsistent with. This is a codebase-wide convention-sweep item, not a defect specific to this file; on its own it does not make the claim as stated \"clearly real\".",
        "Not clearly real; the lead exhibit is a documented deliberate design and the claim's framing is factually off.\n\nPhoto-remove control (src/components/TaskTimeLimitPopup.jsx:202-211) — Chesterton's fence HOLDS. The comment at lines 197-201 (commit 95e02be, a11y sweep) documents why it is hand-rolled: the full 44x44 hit area is anchored `right-0 top-0` INSIDE an `overflow-hidden` 64px thumbnail, with `items-start justify-end` so the ~20px dark circle pins to the corner while the tap area spans inward, and `focus-visible:ring-inset` because an offset ring would be clipped by the tile. IconButton (src/components/ui/IconButton.jsx) cannot express this: it uses plain `clsx` (not the tailwind-merge `cn`), so its baked-in `items-center justify-center rounded-control ... ring-offset-2 active:scale-95` cannot be overridden by a caller `className` — both class sets land and CSS source order wins. The proposed `<IconButton icon={X} .../>` would also drop the bg-black/60 contrast chip and render a bare w-5 X over the photo. Making it fit means changing IconButton, not a drop-in swap. Further, this is the repo-wide pattern, not a lapse unique to this file: all three thumbnail-remove badges are raw <button>s — CompletionPhotoPicker.jsx:63-66 (pseudo-element hit area), TaskModal.jsx:1997-2003, and this one. TaskModal — the claim's own comparison point — imports IconButton yet still hand-rolls its photo-remove button, so \"Unlike the TaskModal.jsx instances\" is wrong for this control.\n\nMute control (273-281) — the only partially defensible half. It reimplements min-h-touch/focus-ring/disabled-opacity that Button already owns, and `<Button variant=\"ghost\" icon={BellOff} disabled={muted}>` is a viable canonical fit (§8 defines ghost as \"text-only\"). But it is not clearly a violation: the hand-rolled version is deliberately quieter than any Button variant (no font-semibold, no hover background, no rounding, h-4 icon, px-3) as a tertiary control centred beneath two full-width stacked CTAs, consistent with §8's \"primary must be visually dominant\" rule; swapping to ghost changes the visual (bolder, hover bg, larger icon). The prior 2026-06-24 re-verify (M5) accepted the OLD mute button as real, but that one was an exact class-for-class clone of Button `secondary`; the popup was since rebuilt (6b4d394) and the mute became a distinct quiet text link with no exact variant match, so the precedent is weaker. No ESLint rule or test bans raw <button>; 129 raw <button>s exist across 41 non-ui files (65 with min-h-touch), so §8 is a preference, not an enforced gate.\n\nHeadline inaccuracy: \"neither is imported\" is false — Button IS imported (line 7) and used five times in this file; only IconButton is absent.\n\nNet: the primary exhibit is a documented intentional geometry the proposed fix would break, the TaskModal contrast is wrong, and the residual mute nit is a low-severity optional polish (ghost Button swap) rather than a clear defect. Default skeptic verdict: real=false.",
        "PARTIALLY REAL — real for the mute button (273-281) only; the photo-remove button (202-211) is a deliberate, commented exception that the proposed IconButton swap would break. Scope any fix to the mute control.\n\nMUTE BUTTON (273-281) — REAL, low/discipline, and a REGRESSION of an already-adjudicated fix. Verified in C:\\Users\\karol\\Desktop\\WORKZ\\.claude\\worktrees\\gallant-moser-71646e\\src\\components\\TaskTimeLimitPopup.jsx: raw <button> with `inline-flex min-h-touch items-center ... disabled:opacity-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-ring focus-visible:ring-offset-2` — byte-identical to the focus/disabled strings in src/components/ui/Button.jsx, whose `ghost` variant is documented as exactly this \"text-only\" case. Button IS imported (line 7) and used 5x in the same file. Chesterton's fence checked and it is NOT a design choice: the 2026-06-24 sweep confirmed this exact control 3/3 as M5 (docs/audits/full-sweep-2026-06-24/00-reasoning-confirmed.md:94) and commit 2e99117 converted it to <Button variant=\"secondary\">; the same-day two-choice rewrite 6b4d394 discarded that and re-hand-rolled a quiet centered text button with no comment or commit-body rationale, even though `ghost` already existed in Button.jsx at that commit (verified via git show 6b4d394:src/components/ui/Button.jsx). The only intentional part is the hierarchy demotion (tertiary below primary/secondary, DESIGN_SYSTEM §8), which `variant=\"ghost\"` expresses directly. The §8 `bare` Modal escape hatch (DESIGN_SYSTEM.md:236-240) covers the popup's full-bleed chrome layout, not its action buttons — the 2026-06-23/24 verifiers reached the same conclusion. No functional/a11y defect today (≥44px, focus ring, disabled state all present), so severity low; `<Button variant=\"ghost\" icon={BellOff} disabled={muted}>` is a safe swap (initialFocusRef targets primaryRef/requestRef, not this button).\n\nPHOTO-REMOVE BUTTON (202-211) — NOT real; deliberate design choice with an explicit signpost, and the claim's \"IconButton would cover it\" is false. The 5-line comment at 197-201 (added by the a11y sweep fix 95e02be, 2026-06-28, whose message explicitly says \"design-system: canonical Button/Card\" — i.e., the author was migrating to canonical components and chose NOT to here) explains the geometry: the tile is `h-16 w-16 overflow-hidden`; the 44px hit area is anchored to the tile's top-right with `items-start justify-end` so the visible 20px black/60 circle stays in the corner while the hit area spans inward, and the focus ring is `ring-inset` so `overflow-hidden` does not clip it. IconButton (src/components/ui/IconButton.jsx) cannot reproduce this: it hardcodes `items-center justify-center` (glyph would land ~22px inward, i.e. mid-tile over the photo), renders the icon bare at w-5 h-5 with no circle backdrop (no contrast guarantee over a photo), paints `hover:bg-surface-sunken` as a 44px grey square over the image, hardcodes `ring-offset-2` (clipped by overflow-hidden), and merges classes with plain `clsx` — not `cn`/tailwind-merge — so `className` overrides of justify/items/ring-inset would leave conflicting utilities both present. No precedent in the codebase uses IconButton as a clipped-tile overlay (ManagerNotifications' `absolute top-2 right-2` IconButtons sit on unclipped Cards). The sibling remove-attachment control in TaskModal.jsx:1996-2003 is likewise a raw button, and 111 raw <button>s remain across 39 component files with no lint gate — the canonical migration is codebase-wide partial debt, not a per-file lapse. Routing this control through IconButton would require extending IconButton's API (a separate design decision), not a call-site swap."
      ]
    },
    {
      "title": "Automatic crash-recovery close of an abandoned secondary session silently drops the whole nested pausedSession stack, with no notice or log (unlike the manager force-end path, which fixes this exact gap)",
      "file": "src/utils/sessionActions.js",
      "line": "389-423",
      "severity": "medium",
      "detail": "endSessionImpl's resume logic is `if (session.pausedSession && !skipResume) { restoredSession = {...session.pausedSession}; ... } else { updates.activeSession = null; }` (lines 389, 421-423). useOrphanedSessionRecovery.js always calls endSession with skipResume=true (line 197: `endSession(uid, freshData, overrides, true)`) when it auto-finalizes an abandoned break/call/quickWork on boot. Because skipResume forces the `else` branch, ANY session nested underneath the abandoned one via `pausedSession` (e.g. a break parked under an interrupting call, or a call parked under a quick-work — sessionNesting.js documents an up-to-2-deep stack) is discarded outright: the field goes straight from a real nested node to `null`, never inspected or reported. The nested session's pre-interruption minutes were already banked when it was first interrupted (see the partial work_sessions write / bankedBreakMinutes fold in startSessionImpl), so no paid time is lost, but (1) the worker's 'return address' — the session they meant to resume — vanishes with zero notice: the only recovery notice fired (`addRecoveryNotice(uid, { kind: 'session', ... })` around line 204-209) reports only the outer session's credited minutes, never the discarded nested one, and (2) for a nested quickWork/call, the `partialDocId` link stored on that pausedSession node (used later by addQuickWorkDescription to retroactively rename the placeholder-titled partial work_sessions row once the worker describes what they did) is destroyed along with it, permanently stranding that row under its generic auto-generated title. Contrast this with sessionAdmin.js's `endSessionForUser` (the manager force-end path), which explicitly computes a `discardedStack`/`legacyDiscarded` list (sessionAdmin.js lines 24-29, 138-147) and notifies the worker exactly what was parked underneath and dropped (lines 38-47, 117-124) — the same 'closing the top of the stack drops everything under it' behavior was deliberately hardened and made visible there, but the automatic crash-recovery path in sessionActions.js never received the equivalent fix. Concretely: a worker on a break takes a phone call (call nests the break via startSession's pausedSession chain); the phone dies mid-call and the app is reopened the next day. useOrphanedSessionRecovery detects the call crossed a Vilnius day, confirms it server-side, and finalizes it via endSession(..., true). The call is logged correctly, but the parked break underneath is wiped to null silently — no notice, no log, worker just appears idle with no trace a break was ever parked, and if that break carried a partialDocId, that partial log row can never be retroactively named. sessionActions.test.js's own coverage of this exact call path (search 'orphan recovery path (useOrphanedSessionRecovery)') never sets up a nested pausedSession, so this silent-drop path is untested.",
      "dimension": "crashsafety",
      "votesReal": 1,
      "votesTotal": 3,
      "confirmed": false,
      "reasons": [
        "Mechanics described are accurate (src/utils/sessionActions.js:389-423 with skipResume=true sets activeSession=null and drops any nested pausedSession; src/hooks/useOrphanedSessionRecovery.js:197 always passes true; the recovery notice at :204-209 carries only outer type+minutes), but this is a deliberate, documented design decision, not a correctness defect — Chesterton's fence is stated in three places. (1) The hook's own JSDoc (useOrphanedSessionRecovery.js:116): \"leaves the user idle (skipResume — there is no live worker to resume a queued task on boot)\". (2) The canonical planner (src/utils/timerTransitionPlan.js:1226-1230 and :769-773): \"skipRestore: end straight to IDLE and leave whatever this session had paused alone. Crash recovery needs it — there is no live worker at boot to hand a resumed task/break back to, so restoring one would silently start a timer nobody is watching. Mirrors the legacy closer's skipResume argument\". (3) planManagerForceEnd (timerTransitionPlan.js:1447-1460) — the very fix the claim cites as the contrast — explicitly discusses the recovery path: it names skipRestore as the recovery-path mechanism and explains WHY force-end notifies while recovery does not (the worker is LIVE and their screen changes from the MANAGER's device; recovery runs on the worker's own device after the whole stack is stale — another Vilnius day or >16h). The authors of abec247 knew about the recovery drop and chose not to extend the notice. The live engine's counterpart (useRevisionedSecondaryRecovery.js → planBreakEnd/planSecondaryEnd with skipRestore:true, `const paused = skipRestore ? null : ...`) behaves identically and emits the same type+minutes-only notice, so this is a consistent cross-engine design rather than one path that \"never received the fix\"; the legacy path the claim targets is also dormant for current bundles (timer engine on for everyone, TIMER_ENGINE_CLIENT_CONTRACT=4). No data harm: pre-interruption time of every nested node is banked at interruption (partial work_sessions row for call/quickWork at sessionActions.js:105-158; break_sessions row + bankedBreakMinutes fold into dailyAccumulatedMinutes at :164-186 and :240-272), and a paused task at the stack bottom was already closed by pauseTask and remains visible as timerStatus 'paused' in the task list — nothing the worker cannot find. Claim point (2), the stranded partialDocId placeholder title, is not recovery-specific: the partial row is renamed ONLY on the live-titled end path (sessionActions.js:817, :931) and addQuickWorkDescription (:966+) never touches partialDocId, so an ordinary deferred-description flow strands it identically — a pre-existing limitation of the partial-row design, outside this claim. The test-coverage observation (sessionActions.test.js:424-443 has no nested pausedSession in the orphan block) is correct but is a missing test for documented behaviour, not a bug. At most this is a UX-copy enhancement (name parked nodes in the recovery notice), which would have to be made in the revisioned planner path too.",
        "Confirmed in source: sessionActions.js:389-423 with skipResume=true takes the else branch and writes activeSession=null, so any nested pausedSession node (break under call, call/quickWork under break, up to the 2-deep stack sessionNesting.js allows) is discarded without being inspected, returned, or logged; useOrphanedSessionRecovery.js:197 always passes skipResume=true and its only notice (lines 204-209) carries {sessionType, minutes, wasCapped} for the outer session alone. Chesterton's fence checked: the DROP itself is deliberate and documented three times (hook header 116-117, planBreakEnd ~769, planSecondaryEnd ~1226 — no live worker at boot to hand a resumed timer to), and the claim does not dispute that. The SILENCE has no documented rationale (no hit in docs/adr or decisions-log), and it contradicts the project's own stated principle in commit abec247 (\"A recovery action that silently deletes the worker's way back is not recovery; it must say what it took\"), which hardened sessionAdmin.endSessionForUser with discardedStack/legacyDiscarded + a session_force_ended parkedSummary. abec247 rejected recoveryNotice as the carrier only because a force-end runs on the MANAGER's device — that objection does not apply here: crash recovery runs on the worker's device and already calls addRecoveryNotice at that exact spot, so the equivalent fix is small and was simply never applied. Test gap confirmed: sessionActions.test.js:424-442 covers skipResume with a flat break + resumableTaskIds, never a nested pausedSession. Severity caveats for the fixer: (1) no paid time is lost — pre-interruption segments are banked in startSessionImpl; (2) the legacy hook is gated off when the revisioned engine owns (WorkerView.jsx:60 legacyRecoveryOwns), so the anchored line is a dormant fallback in prod, BUT useRevisionedSecondaryRecovery -> planBreakEnd/planSecondaryEnd with skipRestore:true drops the stack identically with the same stack-less notice, so the defect class is live in both engines and any fix should cover both; (3) the claim misattributes the partialDocId rename to addQuickWorkDescription — it is actually handleLegacyLogging's call/quickWork branches (sessionActions.js:817, :931) on the resumed session's final end; the consequence (partial row stranded under its generic 'Skambutis'/'Greita veikla' title) is still correct, though the accepted force-end path strands it the same way, so that part is a data-quality nit rather than a loss. Net: real, medium-to-low — a transparency gap the project itself defined as a defect, not a time-integrity bug.",
        "Mechanism is read correctly (skipResume=true forces `updates.activeSession = null` and the recovery notice names only the outer session), but it is not a real crash-safety defect as claimed. (1) Chesterton's fence stands: ending to idle without touching the paused stack is explicitly documented in useOrphanedSessionRecovery.js (\"no live worker to resume a queued task on boot\"), mirrored in timerTransitionPlan.planSecondaryEnd's skipRestore rationale, and cited as the model in the manager force-end commit abec247; no time is lost, nested minutes were banked at interruption, and a paused task stays visible as paused. (2) Not reproducible at the cited location: the legacy closer is gated by `legacyRecoveryOwns = timerEngineResolved && !timerEngineEnabled` (WorkerView.jsx:60, ManagerView.jsx:79); the canonical engine is fleet-wide since 2026-07-29 with minClientContract 4 and this bundle's TIMER_ENGINE_CLIENT_CONTRACT is 4, so sessionActions.js:389-423 never runs on boot recovery for any current client (Firebase MCP was unreachable; based on recorded rollout state). (3) The \"return address\" rationale that justified the manager-path notice does not transfer: recovery only fires when the TOP session crossed a Vilnius day or 16h, so every parked node beneath it is at least as stale — there is nothing actionable to restart, and the worker already gets the auto-closed/credited notice. (4) The partialDocId half is wrong in its example (breaks never carry partialDocId — only the quickWork/call branch at sessionActions.js:152 sets it), applies identically to the \"fixed\" manager force-end path (which also drops it without renaming), and the field exists only in the dormant legacy engine. Residual, mis-located and low severity: the LIVE canonical recovery (useRevisionedSecondaryRecovery → planBreakEnd/planSecondaryEnd with skipRestore:true) also does not name the discarded stack while planManagerForceEnd computes `discardedStack` (timerTransitionPlan.js:1461); at most a UX follow-up adding a parked-summary line to the existing recovery notice there, not a crash-safety fix in sessionActions.js."
      ]
    },
    {
      "title": "Orphan rule: shift_logs has no client (or Cloud Function) reader/writer",
      "file": "firestore.rules",
      "line": "503-505",
      "severity": "low",
      "detail": "match /shift_logs/{logId} { allow read, write: if false; } is explicitly commented '// ---- SHIFT LOGS (unused collection — locked) ----'. A repo-wide search for the string 'shift_logs' across src/ and functions/ turns up zero collection()/doc() references — the only hits are firestore.rules itself and past audit reports (docs/audits/full-sweep-2026-06-2{1,3,4,7}/06-firebase.md) that already flagged it as unused. It is already locked (if false), so there is no live risk, just dead rule surface that could be deleted for clarity.",
      "dimension": "firebase-coupling",
      "votesReal": 0,
      "votesTotal": 3,
      "confirmed": false,
      "reasons": [
        "Facts confirmed: firestore.rules:503-506 contains `// ---- SHIFT LOGS (unused collection — locked) ----` / `match /shift_logs/{logId} { allow read, write: if false; }`, and a grep of src/ and functions/ (excluding node_modules) finds zero references — the finder's factual description is accurate. But it is NOT a correctness issue, and it is a deliberate, documented design choice (Chesterton's fence with a known builder and stated reason):\n\n1. Origin: `shift_logs` was a LIVE production collection. Commit 18a2ca0 (2026-01-12) added it with `allow read, write: if isAuthenticated()` and src/components/WorkDayControls.jsx wrote to it via addDoc/doc. The client writer was removed in e2898bf (2026-06-19). Production may therefore still hold historic shift_logs documents.\n\n2. The `if false` lock was an intentional security decision: commit 4ed91be (2026-06-21, \"fix(security): scope Firestore writes per-owner with manager escape\") explicitly lists \"lock unused collections\" in its body. DEPLOY_FIRESTORE_RULES.md:31 documents the rationale (\"Unused collections (shift_logs, daily_stats) are fully locked ... the client never touches them\"). An explicit deny for a collection that formerly held authenticated-writable data guarantees residual documents stay unreadable regardless of how the rest of the ruleset evolves, and makes that intent legible in the rules file.\n\n3. No behavioural defect exists in either direction: there is no catch-all `/{document=**}` match in firestore.rules, so Firestore default-deny makes `if false` functionally identical to deleting the block. Nothing is wrong, nothing would change — the finder itself concedes \"no live risk\".\n\n4. This exact finding has already been adversarially verified and REJECTED in two prior sweeps: docs/audits/full-sweep-2026-06-23/00-reasoning-confirmed.md:194 (struck through, 0/3 votes) and docs/audits/full-sweep-2026-06-24/_genuinely-rejected.json:105 (0/2 votes); the 2026-06-24 synthesis (00-SYNTHESIS.md:87) records: \"Orphan locked rules (shift_logs, daily_stats) are intentional `if false` placeholders.\" The sibling daily_stats rule at firestore.rules:754-757 follows the identical pattern.\n\nDeleting the block is at most a cosmetic tidy-up preference, not a correctness bug, and it would remove a deliberately placed defensive lock. Verdict: not real.",
        "Factually accurate but not an issue — it is a documented, deliberate design choice (Chesterton's fence with the sign still on it). Verified: firestore.rules:503-506 contains `// ---- SHIFT LOGS (unused collection — locked) ----` + `match /shift_logs/{logId} { allow read, write: if false; }`, and a case-insensitive repo-wide grep (excluding node_modules) finds zero `shift_logs` references in src/ or functions/ — only the rule itself, DEPLOY_FIRESTORE_RULES.md, the triage-sweep finder prompt, and past audit reports. Why it is not real: (1) Intentional and documented — DEPLOY_FIRESTORE_RULES.md:31 states \"Unused collections (shift_logs, daily_stats) are fully locked (read, write: if false) — the client never touches them\", i.e. the lock is an explicit, human-readable statement that the collection is retired, which is worth more to a non-programmer founder auditing rules than an invisible default-deny. (2) Zero behavioral or security consequence — Firestore default-denies any path with no matching rule, so deleting the block yields byte-for-byte identical enforcement; there is no live risk, no eval-budget cost of note, and the rule shape cannot mask a broader match. (3) Already adjudicated three times and rejected each time: 2026-06-23 sweep 0/3 votes, 2026-06-24 sweep 0/2 votes (in _genuinely-rejected.json) with the synthesis concluding \"Orphan locked rules (shift_logs, daily_stats) are intentional `if false` placeholders\", 2026-06-27 06-firebase.md lists it as correct practice. (4) The finding is a prompt echo — .claude/workflows/triage-sweep.js:24 seeds the firebase-coupling finder with the literal example \"orphan rule, e.g. shift_logs, daily_stats\", so the finder is regurgitating its own instruction rather than discovering anything. Its sibling daily_stats rule (firestore.rules:755) is still present too, confirming the pattern is a consistent house convention, not an oversight. Deleting it would be a \"while I'm here\" cosmetic churn to a security-critical, human-only-deploy file for no functional gain, which violates the repo's smallest-viable-change discipline.",
        "The OBSERVATION is accurate and reproducible, but it is not an issue — it is a documented, deliberate design choice that two prior adversarial passes already rejected.\n\nVerified facts (read from source): firestore.rules:503-506 contains exactly `// ---- SHIFT LOGS (unused collection — locked) ----` / `match /shift_logs/{logId} { allow read, write: if false; }`. `grep -rn shift_logs src functions` returns zero hits (exit 1). There is no `document=**` catch-all anywhere in firestore.rules, so Firestore's default-deny already covers the path — the block has zero enforcement effect and removing it would change no runtime behaviour. The claimant concedes this (\"no live risk\").\n\nChesterton's fence — the fence has an owner-stated reason, in three places: (1) the inline comment itself says the author KNEW it was unused and chose an explicit lock over deletion; (2) DEPLOY_FIRESTORE_RULES.md:31 documents it as project policy — \"Unused collections (shift_logs, daily_stats) are fully locked (read, write: if false) — the client never touches them\"; (3) docs/audits/full-sweep-2026-06-24/00-SYNTHESIS.md:87 records the verdict \"Orphan locked rules (shift_logs, daily_stats) are intentional `if false` placeholders.\" The explicit lock is a self-documenting marker that any legacy data in that collection is intentionally inaccessible — cheap defence-in-depth plus intent documentation.\n\nPrior verification history: this exact title was already adversarially voted down twice — 0/3 in full-sweep-2026-06-23 (00-reasoning-confirmed.md:194, struck through) and 0/2 in full-sweep-2026-06-24 (_genuinely-rejected.json:105). The claim's own phrasing (\"past audit reports already flagged it as unused\") misreads those reports: they REJECTED it as a finding, not confirmed it. It keeps resurfacing only because FULL_SWEEP_PLAN.md:339 (§7.6) seeds the finders with `shift_logs` as the canonical example of the pattern.\n\nNet: a style nit (\"could be deleted for clarity\") against an explicitly retained, documented, zero-impact rule. Under the user's smallest-viable-change discipline it is a \"while I'm here\" cleanup, not a defect. Do-not-re-flag candidate."
      ]
    },
    {
      "title": "Orphan rule: daily_stats has no live reader/writer (client stopped using it)",
      "file": "firestore.rules",
      "line": "754-757",
      "severity": "low",
      "detail": "match /daily_stats/{statId} { allow read, write: if false; } is explicitly commented '// ---- DAILY STATS (unused collection — locked) ----'. The only src/ hit is src/components/DailyStatistics.jsx:317-323, where a comment records that the panel no longer reads daily_stats ('we are calculating breaks from break_sessions... let's keep it null for now') and the code does setDailyStats(null); let unsubStats = () => {} (a no-op) instead of ever calling collection(db,'daily_stats'). Same as shift_logs: already locked, zero functional risk, just leftover rule scaffolding for a collection nothing touches.",
      "dimension": "firebase-coupling",
      "votesReal": 0,
      "votesTotal": 3,
      "confirmed": false,
      "reasons": [
        "Factually accurate but not a real correctness issue — it is a deliberate, documented design choice. Verified: firestore.rules:754-757 is `match /daily_stats/{statId} { allow read, write: if false; }` under the explicit heading `// ---- DAILY STATS (unused collection — locked) ----`; grep over src/ and functions/ (excluding node_modules) confirms nothing reads or writes the collection — the only src/ hit is src/components/DailyStatistics.jsx:206/325-330 where `setDailyStats(null)` and a no-op `unsubStats` replace the former listener, with a comment recording that breaks are now derived from break_sessions. No functions/, indexes, scripts or tests reference it. Chesterton's fence stands: (1) the rule's own comment declares the lock intentional; (2) DEPLOY_FIRESTORE_RULES.md:31 documents `shift_logs`/`daily_stats` as intentionally \"fully locked (read, write: if false) — the client never touches them\"; (3) two prior audit sweeps already evaluated and rejected this exact finding (full-sweep-2026-06-23 confirmed list: struck through at 1/3; full-sweep-2026-06-24 synthesis: \"intentional `if false` placeholders\"). Functionally, an explicit `if false` block is identical to having no match block at all (Firestore default-denies unmatched paths), so there is no incorrect behaviour, no security exposure and nothing to fix — removing it would be a cosmetic edit that only discards the documentation of intent and the guard that keeps any legacy documents unreadable if a broader match were ever added. The claim itself concedes \"zero functional risk\"; under the correctness lens that makes it a non-finding.",
        "Facts check out but the \"issue\" is a deliberate, documented design choice, not a defect. Verified in source: firestore.rules:754-757 is `match /daily_stats/{statId} { allow read, write: if false; }` under the comment \"DAILY STATS (unused collection — locked)\"; no `collection(db, 'daily_stats')` call exists anywhere in src/ (DailyStatistics.jsx:206/329-330 only holds a `setDailyStats(null)` + no-op `unsubStats`), and functions/ has zero references. So the rule is indeed orphaned from live code. However, Chesterton's fence has a plaque on it: (1) the lock was introduced on purpose by the security-scoping commit 4ed91be (\"scope Firestore writes per-owner with manager escape\"), not left behind by accident; (2) DEPLOY_FIRESTORE_RULES.md:31 records the intent — \"Unused collections (shift_logs, daily_stats) are fully locked — the client never touches them\"; (3) the 2026-06-24 audit (docs/audits/full-sweep-2026-06-24/06-firebase.md §Info item 2 and 00-SYNTHESIS.md:87) explicitly adjudicated \"Locked/orphan rule collections are intentional, not dead-rule findings\", and the identical finding was rejected 1/3 in the 2026-06-23 sweep (00-reasoning-confirmed.md:195, struck through). Functionally, Firestore denies any path with no matching rule, so `if false` is semantically identical to deleting the block — removing it changes nothing at runtime, while keeping it is a legitimate explicit-deny that documents the lock and prevents a future permissive rule being added to a collection someone assumes is live (any legacy docs still in production stay unreadable). The claim itself concedes \"zero functional risk\". A pure-hygiene note about an intentional, documented, twice-adjudicated placeholder does not meet the bar for a real issue.",
        "The factual observation is accurate (firestore.rules:754-757 locks daily_stats with `allow read, write: if false`; the only src/ reference is a no-op `setDailyStats(null)` at src/components/DailyStatistics.jsx:206/329 and prose comments at 325-328 and 1990-1991; nothing in functions/ touches it). But it is not an issue: the rule is a deliberate, documented defensive lock, not leftover scaffolding. The rule's own comment says \"unused collection — locked\"; DEPLOY_FIRESTORE_RULES.md:31 documents shift_logs/daily_stats as intentionally fully locked; docs/audits/full-sweep-2026-06-24/00-SYNTHESIS.md:87 records them as \"intentional `if false` placeholders\"; and docs/audits/full-sweep-2026-06-23/00-reasoning-confirmed.md:195 shows this identical finding was already adversarially rejected (1/3). An explicit deny on a retired collection that may still hold historical prod documents is a Chesterton's-fence design choice with zero functional risk (the finder concedes this). Re-flagging a settled, documented decision is a false positive."
      ]
    },
    {
      "title": "Raw Firestore/JS err.message rendered verbatim in the Audit dashboard",
      "file": "src/components/AuditDashboard.jsx",
      "line": "313-319",
      "severity": "medium",
      "detail": "`{scanErrors.map((e, i) => (<li ...>{e.scan}: {e.message}</li>))}` prints `e.message` straight into the UI. That message is `err?.message || String(err)` captured verbatim by `noteScanError(scanErrors, scan, err)` in functions/index.js:1659-1660 from whatever Firestore/JS exception the nightly integrity scan hit (e.g. raw Firestore error text such as 'Missing or insufficient permissions' or a JS TypeError message) — never mapped to Lithuanian friendly copy per DESIGN_SYSTEM §10 ('never render raw err.message'). Every other error surface in this codebase (Reports.jsx, TaskTable.jsx, DailyStatistics.jsx, TaskTimerControls.jsx, Login.jsx) explicitly maps error codes to Lithuanian sentences and comments 'never raw err.message' — this is the one place in src/ where a raw, potentially-English exception message is displayed directly to a (admin) user instead.",
      "dimension": "i18n-brand",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "DEV-only login error surface falls back to raw err.message and an English literal",
      "file": "src/pages/Login.jsx",
      "line": "149",
      "severity": "low",
      "detail": "`setDevError(err?.code || err?.message || 'Dev login failed')` — rendered at line 222 (`{devError && <p role=\"alert\">{devError}</p>}`) inside the 'DEV testavimas' panel on the Login page. This both (a) can surface a raw Firebase `err.message` string and (b) falls back to the English literal 'Dev login failed', violating both the 'never render raw err.message' and 'no English leakage in user-facing copy' rules (DESIGN_SYSTEM §10). It is gated by `import.meta.env.DEV` and tree-shaken from production, but the panel is the app's own documented visual-QA login path (docs/runbooks/visual-qa-test-account.md), so a human does see this text during every dev-mode QA session.",
      "dimension": "i18n-brand",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "Duplicate always-on onSnapshot listener for the same tasks query",
      "file": "src/hooks/useUndescribedQuickWork.js",
      "line": "53-85",
      "severity": "medium",
      "detail": "Layout.jsx unconditionally mounts <QuickWorkDescribePrompt /> for every signed-in user (src/components/Layout.jsx:177), which calls useUndescribedQuickWork(currentUser). That hook opens its own onSnapshot on query(collection(db,'tasks'), where('assignedUserId','==', uid)) with no limit(). WorkerView.jsx, rendered for that same worker session, opens a SEPARATE onSnapshot on the identical query (src/pages/WorkerView.jsx:171-176, same collection + same where clause, also no limit). Both listeners run concurrently for the whole session; each is individually cleaned up correctly, so this is not a leak, but it is a duplicate always-on subscription: every worker pays for two live Firestore listeners over the exact same document set instead of one, doubling the initial read cost and doubling re-delivery cost on every write to any of that worker's tasks (a status change, a heartbeat write, etc). Failure scenario: a worker with N assigned task docs opens WorkerView (the normal worker screen); Firestore streams the same N docs twice on load and re-streams them twice on every relevant write, for as long as the app stays open.",
      "dimension": "perf",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "Sequential per-day Firestore round-trips instead of a batch when copying/submitting calendar days",
      "file": "src/components/WorkPlanner.jsx",
      "line": "466-490",
      "severity": "high",
      "detail": "executeDirectCalendarUpdate (src/components/WorkPlanner.jsx:507-565) performs up to 4 sequential awaited Firestore calls per invocation: addDoc/updateDoc/deleteDoc on work_hours, then addDoc on calendar_requests, then logCalendarChange (src/utils/calendarNotifications.js:26-36+) which itself does a getDoc followed by an updateDoc/setDoc on calendar_notifications. handleCopyWeekForward (WorkPlanner.jsx:466-490, 'for (const ev of sourceWeekEvents) { ... await executeDirectCalendarUpdate(...) }') and the manual multi-day submit path (WorkPlanner.jsx:771-775, 'for (const d of days) { await executeDirectCalendarUpdate(...) }') both call this function once per day INSIDE a for-loop, awaiting each iteration before starting the next, rather than using a writeBatch the way the sibling approval-request path does (WorkPlanner.jsx:928-940 correctly batches with batch.set). Failure scenario: tapping 'Copy week forward' with a full 7-day source week (or submitting a multi-day manual span) issues up to 28 sequential network round-trips (7 days x ~4 calls each) before the UI's 'submitting' state clears, each one waiting on the previous — a multi-second block on a slow/mobile connection, plus needless Firestore read+write billing versus one batched commit.",
      "dimension": "perf",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "Whole-collection tasks query with no limit() for unscoped managers/admins",
      "file": "src/hooks/useManagerData.js",
      "line": "38-40",
      "severity": "low",
      "detail": "For an unscoped manager or admin, the always-mounted manager task listener runs query(collection(db,'tasks'), orderBy('createdAt','desc')) with no limit() or pagination — every open task in the whole company is downloaded and kept live for the lifetime of the manager's session. The same unbounded shape recurs in src/components/CombinedHoursSummary.jsx:79 (query(collection(db,'tasks')) with no limit, though at least gated behind the panel's collapsed state) and src/components/DailyStatistics.jsx:366 (activeQ = collection(db,'tasks') for the whole-team branch, no limit). Nightly archival of completed tasks keeps this collection's steady-state size bounded today, but there is no query-level ceiling, so the read cost (and initial payload) grows linearly with how many tasks are simultaneously open company-wide, with no page size cap to keep a single manager session's listener cost predictable as the company grows.",
      "dimension": "perf",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "\"Current security model\" section omits ~2.5 months of rules changes",
      "file": "DEPLOY_FIRESTORE_RULES.md",
      "line": "8-38",
      "severity": "high",
      "detail": "The doc's \"Current security model (as of the 2026-06-21 hardening)\" describes only a plain per-document-owner + manager/admin-escape model. It has no mention of the scoped-manager hierarchy (`teamManagerIds`/`overseerIds`/`seniorManagerIds`/`scopedManager`, ADR 0005/0007 — present in firestore.rules lines 57-100 and 366-434), tiered pay rates (`payRate`, ADR 0012 — firestore.rules lines 374, 434), `durationMinutes` shape/range validation (ADR 0011 — firestore.rules lines 200-236), or the `decision_log` collection/audit rule (ADR 0015 — firestore.rules match block at line 1107). All of these exist in the current firestore.rules but are invisible in this doc's summary of what the live rules enforce.",
      "dimension": "docsdrift",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "\"Active tailwind.config.js\" code sample in tokens.md no longer matches the real file",
      "file": "docs/design/tokens.md",
      "line": "344-365",
      "severity": "high",
      "detail": "Section 8 (introduced as \"Wired per ADR 0001\") shows a `theme.extend` snippet where `brand`, `surface`, `ink`, `line`, `session.*`, and `feedback.*` are literal hex strings (e.g. `brand: { DEFAULT: '#4F46E5', ... }`, `session: { quickWork: { shell: '#DC2626', ... } } }`). The actual tailwind.config.js (lines 24-134) implements every one of these groups as CSS-variable references via `withAlpha('--x')` (`rgb(var(--x) / <alpha-value>)`) per ADR 0008 (user-selectable theme) and ADR 0016 (theme-reactive session colors) — both of which tokens.md's own §0 correctly describes. Only the `tier` block's `surface`/`accent`/`ring` (still literal hex, `label` variable-backed) matches the snippet. The §8 sample is a pre-theming snapshot that was never updated after theming was wired in.",
      "dimension": "docsdrift",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "DESIGN_SYSTEM.md claims ~150 banned sub-12px text usages remain; zero do",
      "file": "docs/design/DESIGN_SYSTEM.md",
      "line": "137-139",
      "severity": "medium",
      "detail": "\"The ~150 existing uses are legacy debt to migrate up\" (referring to `text-[8px]/[9px]/[10px]/[11px]`). A repo-wide search for `text-[8px]`, `text-[9px]`, `text-[10px]`, `text-[11px]` under src/ turns up zero actual usages — the only hit is a code comment in src/components/TaskHistory.jsx:33 noting a field \"was text-[10px]\" before being fixed. The migration this line describes as outstanding appears to already be complete.",
      "dimension": "docsdrift",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "CLAUDE.md references a /deploy-netlify command that doesn't exist in this repo",
      "file": "CLAUDE.md",
      "line": "84",
      "severity": "medium",
      "detail": "\"Existing: `/ship`, `/deploy-netlify`, `/firebase-status`.\" This repo's .claude/commands/ only contains debug.md, firebase-status.md, full-debug-sweep.md, lost-time-triage.md, and ship.md — no deploy-netlify.md, and `git log --name-status -- .claude/commands/` shows one was never added. (A globally-available `/deploy-netlify` skill exists, but it targets an unrelated project, GODSGLOOM, not WORKZ.) The line was introduced in commit d3ad310 and appears to have been copied/aspirational rather than reflecting an actual WORKZ command.",
      "dimension": "docsdrift",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "Stale file:line citations in the R-04/R-06 closure roadmap",
      "file": "docs/roadmap/r04-r06-closure-roadmap.md",
      "line": "12-14",
      "severity": "low",
      "detail": "Cites `AuthContext.jsx:404` for where the client reads the `timerEngineEnabled` flag and `AuthContext.jsx:33` for where it \"defaults timerEngineEnabled=false\", plus `WorkerView.jsx:75`. In the current source, the `timerEngineEnabled` computation is at src/context/AuthContext.jsx:606 (derived from `timerEngineStatus`, whose `'unknown'` default state is declared at line 54), and line 404 is now unrelated comment text about account-deletion listener teardown. The referenced code and behavior still exist, but the pinpoint line references have drifted and no longer locate the described logic.",
      "dimension": "docsdrift",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "DESIGN_SYSTEM.md flags the call-timer blue/sky drift as a live bug; already fixed",
      "file": "docs/design/DESIGN_SYSTEM.md",
      "line": "98",
      "severity": "low",
      "detail": "\"call is blue, never sky — unify CallTimer onto this ... (today the call state is blue in some places and sky in others — that is a bug to fix)\". A repo-wide search for `sky-` classes in src/ (and for the CallTimer component specifically) finds none — src/components/CallTimer.jsx now consistently uses the `session-call-*` tokens (`bg-session-call-surface`, `text-session-call-accent`, etc.). The bug this note flags appears to already be resolved.",
      "dimension": "docsdrift",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "toggleTaskCompletion / entire taskCompletionActions.js orphaned — never wired to any UI",
      "file": "src/utils/taskCompletionActions.js",
      "line": "1-33",
      "severity": "medium",
      "detail": "The module's sole export `toggleTaskCompletion` (line 17) is described in its own docstring and in taskCompletionActions.test.js as 'the checkbox/\"Užbaigti\" orchestrator' for completing/un-completing a task via a checkbox, routing to the audited completeTask/reopenTask domain commands. No component (TaskCard.jsx, TaskTable.jsx, task/TaskRow.jsx, TaskModal.jsx) imports or calls it — grepping the whole src tree only turns up the function's own definition and its dedicated test file. The actual completion UI uses a separate button-driven 'Užbaigti ir priduoti' flow (TaskCard.jsx:360, TaskTable.jsx:419) instead. This looks like a built-but-never-integrated feature; the file plus its 15-line characterization test are dead weight.",
      "dimension": "deadcode",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "archiveTask() dead — superseded by the audited deleteTask domain command",
      "file": "src/utils/taskActions.js",
      "line": "636-657",
      "severity": "medium",
      "detail": "`archiveTask(task, userId)` manually moves a task from `tasks` to `archived_tasks` via a raw setDoc/deleteDoc pair, bypassing decision-log auditing. It has zero callers anywhere in src/, functions/, or tests. The live archive path is `src/domain/commands/deleteTask.js` (the audited command used by TaskHistory.jsx, TaskCard.jsx, TaskTable.jsx, etc., per ADR 0015), which already writes to `archived_tasks` with proper auditing. `archiveTask` appears to be a pre-migration leftover that should have been deleted alongside the ADR 0015 domain-command rollout.",
      "dimension": "deadcode",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "isTaskTimerAnomalous / TASK_TIMER_ANOMALY_RATIO never called — superseded by computeDataTrust",
      "file": "src/utils/timeUtils.js",
      "line": "228,238-253",
      "severity": "low",
      "detail": "This exported function is extensively documented (referencing a real production incident, 'the 145h photo task') as backing a manager-facing '⚠ Patikrinti' affordance, but it is never imported or invoked anywhere in src/ or functions/ — not even from DailyStatistics.jsx, whose own comment at line 1440 says 'computeDataTrust counts the same — and stricter — set' (the actual live implementation, in src/utils/reportAggregate.js:147). This reads as an earlier anomaly-detection approach that was superseded by computeDataTrust but never removed.",
      "dimension": "deadcode",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "parseTimeToHours has no callers anywhere",
      "file": "src/utils/formatters.js",
      "line": "27-51",
      "severity": "low",
      "detail": "Exported with full JSDoc claiming it is 'Centralized ... to eliminate duplication across components,' but no component, hook, util, or test imports `parseTimeToHours` — grep across the whole src tree only matches its own definition and its own doc-comment examples. Dead utility function.",
      "dimension": "deadcode",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    },
    {
      "title": "shift_logs Firestore collection has no client reader/writer",
      "file": "firestore.rules",
      "line": "503-506",
      "severity": "low",
      "detail": "`match /shift_logs/{logId} { allow read, write: if false; }` is already commented '(unused collection — locked)' and confirmed: no code in src/ or functions/ references `shift_logs` in any form. Since it is already locked to deny-all, this is not a security risk, but the entry (and comment) is inert and could be deleted outright rather than kept as a placeholder — flagging per the requested check for orphan rules entries.",
      "dimension": "deadcode",
      "votesReal": 0,
      "votesTotal": 0,
      "confirmed": false,
      "reasons": []
    }
  ]
}